
#          @@@@@@@@@@
#        @@@@@@@@@@@@
#       @@@@@
#       @@@@
# @@@@@@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@@@@@
#       @@@@
#       @@@@
#       @@@@
#       @@@@
#       @@@@
#       @@@@
# @@@@@@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@@@@@

# Open Source LiteGram plugin for https://exteragram.app
# Plugin created by t.me/wepinek
# Licensed under the MIT License
# Repository: https://github.com/nonFeature/LegacyGram

_BK='_settings_cache'
_BJ='searchEmoji'
_BI='TL_messages_getStickerSet'
_BH='TL_messages_getStickers'
_BG='TL_messages_getRecentStickers'
_BF='TL_messages_searchStickers'
_BE='TL_messages_getFeaturedStickers'
_BD='org.telegram.ui.Components.ReactionsContainerLayout'
_BC='java.util.ArrayList'
_BB='isEmojiStatusCollectible'
_BA='org.telegram.ui.Cells.ChatMessageCell'
_B9='showTranslateChatButton'
_B8='PeerColorGrid'
_B7='HeaderCellColorPreview'
_B6='folder tag'
_B5='tag color'
_B4='цвет тега'
_B3='org.telegram.ui.Components.StickerCategoriesListView'
_B2='onBindViewHolder'
_B1='onCreateViewHolder'
_B0='messageOwner'
_A_='org.telegram.tgnet.tl.TL_stars$TL_starGiftUnique'
_Az='com.exteragram.messenger.preferences.utils.SettingsRegistry'
_Ay='Hide in Search'
_Ax='Скрыть в поиске'
_Aw='Скрыть Истории'
_Av='Скрыть "Отправить подарок"'
_Au='manage_settings_options'
_At='stickers'
_As='documents'
_Ar='userInfo'
_Aq='fillItems'
_Ap='viewType'
_Ao='rowCount'
_An='org.telegram.messenger.MediaDataController'
_Am='org.telegram.ui.Components.ChatActivityEnterView'
_Al='restart'
_Ak='restart_required'
_Aj='github_sub'
_Ai='github_repository'
_Ah='about_plugin'
_Ag='hide_ai_summarize'
_Af='hide_ai_button'
_Ae='ai_features_header'
_Ad='hide_articles_editor'
_Ac='hide_premium_features_sub'
_Ab='hide_premium_features'
_Aa='premium_features_header'
_AZ='hide_group_stickers'
_AY='premium_stickers_settings_header'
_AX='premium_emoji_settings_header'
_AW='hide_emoji_search'
_AV='hide_star_reaction'
_AU='hide_stars_rating'
_AT='hide_collectible_status_sub'
_AS='hide_collectible_status'
_AR='hide_giveaway_cards'
_AQ='hide_gift_cards'
_AP='hide_bottom_gift_button'
_AO='hide_action_bar_clear_history'
_AN='hide_action_bar_report'
_AM='hide_action_bar_add_shortcut'
_AL='hide_action_bar_boost_group'
_AK='hide_action_bar_send_gift'
_AJ='hide_action_bar_archived_stories'
_AI='hide_action_bar_live_stream'
_AH='action_bar'
_AG='hide_profile_business_sub'
_AF='hide_profile_business'
_AE='hide_profile_music'
_AD='hide_bot_verification'
_AC='hide_premium_badge_sub'
_AB='hide_premium_badge'
_AA='hide_boost_badge'
_A9='hide_profile_colorful_background'
_A8='hide_profile_pinned_gifts'
_A7='hide_profile_background_emoji_sub'
_A6='hide_profile_background_emoji'
_A5='manage_reply_elements'
_A4='profile_appearance'
_A3='hide_gifts_tab'
_A2='hide_stories_tab'
_A1='profile_tabs'
_A0='hide_profile_actions_stream_button'
_z='hide_profile_actions_stories_button'
_y='hide_profile_actions_gift_button'
_x='profile_buttons'
_w='hide_greeting_button_sub'
_v='hide_greeting_button'
_u='hide_action_bar_status'
_t='hide_stories'
_s='chat_list'
_r='manage_drawer_options'
_q='drawer_options'
_p='settings_options'
_o='plugin_description'
_n='category_about'
_m='category_interface'
_l='category_profile'
_k='category_chat'
_j='hide_help_section'
_i='hide_send_a_gift_row'
_h='hide_business_row'
_g='hide_wallet_row'
_f='hide_ton_row'
_e='hide_stars_row'
_d='hide_premium_row'
_c='hide_premium_stickers_grid'
_b='hide_premium_stickers_search'
_a='hide_premium_stickers_recent'
_Z='hide_premium_suggestions'
_Y='hide_premium_search'
_X='hide_premium_emoji_reactions'
_W='hide_premium_emoji_keyboard'
_V='premium'
_U='animated_'
_T='org.telegram.ui.ProfileActivity'
_S='org.telegram.messenger.MessageObject'
_R='org.telegram.messenger.DialogObject'
_Q='org.telegram.messenger.UserObject'
_P='main_tab'
_O='updateRows'
_N='id'
_M='folderTags'
_L='items'
_K='org.telegram.messenger.MessagesController'
_J='org.telegram.ui.ChatActivity'
_I='gifts'
_H='type'
_G='switch_all'
_F='en'
_E='set'
_D='extera_outline'
_C=True
_B=False
_A=None
import time,weakref
from android.view import View
from base_plugin import BasePlugin,HookResult,MethodHook,MethodReplacement
from client_utils import get_last_fragment
from collections.abc import Callable
from com.exteragram.messenger.plugins.models import HeaderSetting
from de.robv.android.xposed import XposedBridge
from hook_utils import find_class,get_private_field,set_private_field
from java import jint
from java.lang import Boolean,Runtime
from java.lang.reflect import Modifier
from org.telegram.messenger import R as R_tg
from org.telegram.ui.Components import UItem
from typing import Any,Optional
from ui.bulletin import BulletinHelper
from ui.settings import Divider,Header,Switch as BaseSwitch,Text
__name__='LiteGram'
__description__='A highly customizable plugin to **remove paid and bloated features** from Telegram.'
__version__='1.6'
__id__='litegram'
__author__='fork: @nonPlugins, origin author: @wepinek'
__icon__='feature_plugins/8'
__min_version__='12.1.1'
GITHUB_URL='https://github.com/nonFeature/LiteGram'
class Keys:switch_all='switchAll';hide_premium_row='hidePremiumRow';hide_stars_row='hideStarsRow';hide_ton_row='hideTonRow';hide_wallet_row='hideWalletRow';hide_business_row='hideBusinessRow';hide_send_a_gift_row='hideSendAGiftRow';hide_help_section='hideHelpSection';chatSettings='chatSettings';profileSettings='profileSettings';interfaceSettings='interfaceSettings';aboutSettings='aboutSettings';drawer_options='drawerSettings';hide_profile_actions_stream_button='hideProfileActionsStreamButton';hide_stories='hideStories';hide_action_bar_status='hideActionBarStatus';hide_greeting_button='hideGreetingButton';hide_group_stickers='hideGroupStickers';hide_profile_actions_stories_button='hideProfileActionsStoriesButton';hide_profile_actions_gift_button='hideProfileActionsGiftButton';hide_stories_tab='hideStoriesTab';hide_gifts_tab='hideGiftsTab';reply_elements='replyElements';hide_profile_background_emoji='hideProfileBackgroundEmoji';hide_profile_pinned_gifts='hideProfilePinnedGifts';hide_profile_colorful_background='hideProfileColorfulBackground';hide_boost_badge='hideBoostBadge';hide_profile_music='hideProfileMusic';hide_profile_business='hideProfileBusiness';hide_premium_emoji_keyboard='hidePremiumEmojiKeyboard';hide_premium_emoji_reactions='hidePremiumEmojiReactions';hide_premium_search='hidePremiumSearch';hide_premium_suggestions='hidePremiumSuggestions';switch_all_premium_emoji='switchAllPremiumEmoji';PREMIUM_EMOJI_ROWS=(hide_premium_emoji_keyboard,_W),(hide_premium_emoji_reactions,_X),(hide_premium_search,_Y),(hide_premium_suggestions,_Z);hide_premium_stickers_recent='hidePremiumStickersRecent';hide_premium_stickers_search='hidePremiumStickersSearch';hide_premium_stickers_grid='hidePremiumStickersGrid';switch_all_premium_stickers='switchAllPremiumStickers';PREMIUM_STICKERS_ROWS=(hide_premium_stickers_recent,_a),(hide_premium_stickers_search,_b),(hide_premium_stickers_grid,_c);hide_premium_badge='hidePremiumBadge';hide_bot_verification='hideBotVerification';hide_premium_features='hidePremiumFeatures';hide_articles_editor='hideArticlesEditor';hide_ai_button='hideAiButton';hide_ai_summarize='hideAiSummarize';hide_action_bar_live_stream='hideActionBarLiveStream';hide_action_bar_archived_stories='hideActionBarArchivedStories';hide_action_bar_send_gift='hideActionBarSendGift';hide_action_bar_boost_group='hideActionBarBoostGroup';hide_action_bar_add_shortcut='hideActionBarAddShortcut';hide_action_bar_report='hideActionBarReport';hide_action_bar_clear_history='hideActionBarClearHistory';hide_bottom_gift_button='hideBottomGiftButton';hide_gift_cards='hideGiftCards';hide_giveaway_cards='hideGiveawayCards';hide_collectible_status='hideCollectibleStatus';hide_stars_rating='hideStarsRating';hide_star_reaction='hideStarReaction';hide_emoji_search='hideEmojiSearch';SETTINGS_OPTION_ROWS=(hide_premium_row,_d),(hide_stars_row,_e),(hide_ton_row,_f),(hide_wallet_row,_g),(hide_business_row,_h),(hide_send_a_gift_row,_i),(hide_help_section,_j)
_Locale=_A
_cached_lang=_A
def get_system_language():
	global _Locale,_cached_lang
	if _cached_lang is not _A:return _cached_lang
	try:
		if _Locale is _A:_Locale=find_class('java.util.Locale')
		if not _Locale:_cached_lang=_F;return _cached_lang
		A=_Locale.getDefault().getLanguage();_cached_lang=A if A in STRINGS else _F;return _cached_lang
	except Exception:pass
	_cached_lang=_F;return _cached_lang
def t(key,*B):
	C=get_system_language();D=STRINGS.get(C,STRINGS.get(_F,{}));A=D.get(key)
	if A is _A:return f"MISSING: {key}"
	return A.format(*B)
STRINGS={'ru':{_k:'Чат',_l:'Профиль',_m:'Интерфейс',_n:'О плагине',_o:'Гибко настраиваемый плагин для удаления платных и лишних функций из Telegram',_p:'Меню настроек',_Au:'Настройка меню настроек',_G:'Переключить все',_d:'Скрыть "Telegram Premium"',_e:'Скрыть "Мои Звёзды"',_f:'Скрыть "Мои TON"',_g:'Скрыть "Wallet"',_h:'Скрыть "Telegram для бизнеса"',_i:_Av,_j:'Скрыть раздел "Помощь"',_q:'Боковое меню',_r:'Настройка бокового меню',_s:'Список чатов',_t:_Aw,_u:'Скрыть статус',_v:'Скрыть промо Telegram Business в новых чатах',_w:'Убирает кнопку с текстом "имя установил(а) это сообщение для всех новых чатов"',_x:'Кнопки в профиле',_y:'Скрыть кнопку отправки подарка',_z:'Скрыть кнопку новой истории',_A0:'Скрыть кнопки трансляции и видеочата',_A1:'Вкладки в профиле',_A2:_Aw,_A3:'Скрыть Подарки',_A4:'Оформление профиля',_A5:'Настройка оформления ответов',_A6:'Скрыть фоновый эмодзи',_A7:'Те, что вокруг аватара',_A8:'Скрыть подарки вокруг аватара',_A9:'Скрыть цветной фон',_AA:'Скрыть значок буста',_AB:'Скрыть премиум значок',_AC:'Также отключает видеоаватары в списке чатов и чате',_AD:'Скрыть значок верификации от организации',_AE:'Скрыть музыкальный трек',_AF:'Скрыть бизнес-информацию',_AG:'Гео/часы работы',_AH:'Три точки',_AI:'Скрыть "Начать трансляцию" и "Видеочат"',_AJ:'Скрыть "Архив историй"',_AK:_Av,_AL:'Скрыть "Голоса"',_AM:'Скрыть "Создать ярлык"',_AN:'Скрыть "Пожаловаться"',_AO:'Скрыть "Очистить историю"',_I:'Элементы чата',_AP:'Скрыть кнопку подарка в поле ввода',_AQ:'Скрыть карточки подарков в чате',_AR:'Скрыть розыгрыши в чатах',_AS:'Скрыть collectible-статус',_AT:'Если в статусе стоит подарок',_AU:'Скрыть рейтинг звёзд',_AV:'Скрыть реакции за звёзды',_AW:'Скрыть эмодзи в поле поиска',_AX:'Скрытие кастомных эмодзи',_W:'Скрыть в клавиатуре',_X:'Скрыть в реакциях',_Y:_Ax,_Z:'Скрыть в подсказках',_AY:'Скрытие премиум-стикеров',_a:'Скрыть из недавних',_b:_Ax,_c:'Скрыть на панели выбора',_AZ:'Скрыть стикерпак группы',_Aa:'Премиум-Функции',_Ab:'Скрыть премиум-функции',_Ac:'Скрывает некоторые настройки конфиденциальности, теги папок, и перевод чатов',_Ad:'Скрыть редактор статей',_Ae:'ИИ-Функции',_Af:'Скрыть кнопку "AI" в поле ввода',_Ag:'Скрыть кнопку саммари',_Ah:'О Плагине',_Ai:'Репозиторий на Github (Поставьте ⭐!)',_Aj:'Никакой телеметрии, модульная архитектура на Python, кастомный скрипт для сборки, Ruff-линтинг и типизация для java, xposed и android модулей',_Ak:'Перезапустите приложение, чтобы изменения вступили в силу.',_Al:'ПЕРЕЗАПУСТИТЬ'},_F:{_k:'Chat',_l:'Profile',_m:'Interface',_n:'About',_o:'A highly customizable plugin to remove paid and bloated features from Telegram',_p:'Settings Options',_Au:'Manage Settings Options',_G:'Switch All',_d:'Hide "Telegram Premium"',_e:'Hide "My Stars"',_f:'Hide "My TON"',_g:'Hide "Wallet"',_h:'Hide "Telegram Business"',_i:'Hide "Send a Gift"',_j:'Hide "Help" Section',_q:'Drawer Options',_r:'Manage Drawer Options',_s:'Chat List',_t:'Hide Stories',_u:'Hide Status',_v:'Hide Telegram Business Promo in Empty Chats',_w:'Removes button with text "name added the message above for all empty chats"',_x:'Profile Buttons',_y:'Hide Gift Button',_z:'Hide Stories Button',_A0:'Hide Live Stream & Video Chat Button',_A1:'Profile Tabs',_A2:'Hide Stories Tab',_A3:'Hide Gifts Tab',_A4:'Profile Appearance',_A5:'Manage Reply Elements',_A6:'Hide Profile Icons',_A7:'Those around the avatar',_A8:'Hide Gifts Around Avatar',_A9:'Hide Colorful Background',_AA:'Hide Boost Badge',_AB:'Hide Premium Badge',_AC:'Also disables video avatars in chat lists and chats',_AD:'Hide organization verification badge',_AE:'Hide music track',_AF:'Hide business info',_AG:'Geo/working hours',_AH:'Three dots',_AI:'Hide "Start Live Stream" & "Start Voice Chat"',_AJ:'Hide "Archived Stories"',_AK:'Hide "Send Gift"',_AL:'Hide "Boost Group"',_AM:'Hide "Add to Home Screen"',_AN:'Hide "Report"',_AO:'Hide "Clear History"',_I:'Chat Elements',_AP:'Hide Gift Button from Input Field',_AQ:'Hide Gift Cards in Chat',_AR:'Hide Giveaways',_AS:'Hide Collectible Status',_AT:'If there is a gift in the status',_AU:'Hide Stars Rating',_AV:'Hide Star Reaction',_AW:'Hide emoji in search field',_AX:'Hide Custom Emoji',_W:'Hide in Keyboard',_X:'Hide in Reactions',_Y:_Ay,_Z:'Hide in Suggestions',_AY:'Hide Premium Stickers',_a:'Hide from Recents',_b:_Ay,_c:'Hide in Picker Grid',_AZ:'Hide Group Sticker Pack',_Aa:'Premium Features',_Ab:'Hide premium features',_Ac:'Hides some privacy settings, folder tags, and chat translation',_Ad:'Hide articles editor',_Ae:'AI Features',_Af:'Hide "AI" Button in Message Input',_Ag:'Hide Summarize Button',_Ah:'About Plugin',_Ai:'Github Repository (Leave a ⭐!)',_Aj:'Zero Telemetry, Python modular architecture with custom build script, Ruff linting, and typing stubs for java, Xposed and android modules',_Ak:'Restart the app for changes to take effect.',_Al:'RESTART'}}
_cached_parsed_version=_A
def _parsed_version():
	global _cached_parsed_version
	if _cached_parsed_version is _A:_cached_parsed_version=parse_version(get_client_version())
	return _cached_parsed_version
def open_extera_setting(alias,plugin_id=_A):
	A=alias
	try:
		B=find_class(_Az)
		if not B:return
		A=resolve_extera_function(A);C=B.getInstance();C.handleLink(A,plugin_id)
	except Exception as D:BulletinHelper.show_error(f"Failed to open extera setting: {D}")
def resolve_extera_function(function_name):
	A=function_name
	try:
		B=_parsed_version()
		if A==Keys.drawer_options:
			if B>=(12,4,1):return'appNavigationSettings'
			return'myProfileItem'
	except Exception:pass
	return A
def resolve_icon(icon_name):
	C='etg_settings';A=icon_name
	try:
		B=_parsed_version()
		if A==_D and B<(12,4,1):return C
		if A==C and B>=(12,4,1):return _D
	except Exception:pass
	return A
def Switch(key,text,default=_B,subtext=_A,icon=_A,on_change=_A,on_long_click=_A,link_alias=_A):A=link_alias;A=key if A is _A else A;return BaseSwitch(key=key,text=text,default=default,subtext=subtext,icon=icon,on_change=on_change,on_long_click=on_long_click,link_alias=A)
def toggle_group_options(row_keys,default_val=_B):
	A=row_keys;B=LiteGramPlugin.get_instance();C=any(not bool(B.get_setting(A,default_val))for A in A)
	for(D,E)in enumerate(A):B.set_setting(E,C,reload_settings=D==len(A)-1)
def toggle_settings_options(_=_A):toggle_group_options([A for(A,B)in Keys.SETTINGS_OPTION_ROWS])
def toggle_premium_emoji_options(_=_A):toggle_group_options([A for(A,B)in Keys.PREMIUM_EMOJI_ROWS],default_val=_C)
def toggle_premium_stickers_options(_=_A):toggle_group_options([Keys.hide_premium_stickers_recent,Keys.hide_premium_stickers_search,Keys.hide_premium_stickers_grid],default_val=_C)
def open_extera_tab(tab_name):
	def A(view):open_extera_setting(tab_name)
	return A
def open_url_view(url):
	def A(view):open_url(url)
	return A
def show_restart_bulletin(_enabled):BulletinHelper.show_with_button(text=t(_Ak),button_text=t(_Al),icon_res_id=R_tg.raw.info,on_click=lambda:restart_app())
def show_restart_bulletin_if_disabled(enabled):
	if not enabled:show_restart_bulletin(_B)
def reload_ui_on_change(key):
	def A(enabled):LiteGramPlugin.get_instance().set_setting(key,enabled,reload_settings=_C)
	return A
def reload_ui_and_show_restart_bulletin_if_disabled(key):
	def A(enabled):
		A=enabled;LiteGramPlugin.get_instance().set_setting(key,A,reload_settings=_C)
		if not A:show_restart_bulletin(_B)
	return A
_cached_version=_A
def get_client_version():
	global _cached_version
	if _cached_version is not _A:return _cached_version
	try:
		A=find_class('org.telegram.messenger.BuildVars')
		if A:_cached_version=str(A.BUILD_VERSION_STRING);return _cached_version
	except Exception:pass
	_cached_version='Unknown';return _cached_version
def parse_version(version):
	try:return tuple(map(int,version.split('.')))
	except ValueError:return 0,0,0
def open_url(url):
	try:
		A=get_last_fragment()
		if not A:return
		B=A.getParentActivity()
		if not B:return
		C=find_class('org.telegram.messenger.browser.Browser')
		if C:C.openUrl(B,url)
	except Exception as D:BulletinHelper.show_error(f"Failed to open url {url}: {D}")
def restart_app():
	B=get_last_fragment()
	if not B:return
	A=B.getParentActivity()
	if A:C=A.getPackageName();E=A.getPackageManager();F=E.getLaunchIntentForPackage(C);G=find_class('android.content.Intent');D=G.makeRestartActivityTask(F.getComponent());D.setPackage(C);A.startActivity(D);Runtime.getRuntime().exit(0)
class BaseHook(MethodHook):
	def __init__(A,plugin,setting_key=_A):A.plugin=plugin;A.setting_key=setting_key
	def is_enabled(A):
		if not A.setting_key:return _C
		return bool(A.plugin.get_setting(A.setting_key,_B))
ADD_SHORTCUT_PROFILE=14
CALL_ITEM=15
GIFT_PREMIUM=38
CHANNEL_STORIES=39
REPORT_PROFILE=24
CLEAR_HISTORY_CHAT=15
REPORT_CHAT=21
ADD_SHORTCUT_CHAT=24
BOOST_GROUP=29
CLEAR_HISTORY_DIALOGS=103
_ITEM_KEY_MAP={CALL_ITEM:Keys.hide_action_bar_live_stream,CLEAR_HISTORY_CHAT:Keys.hide_action_bar_clear_history,CLEAR_HISTORY_DIALOGS:Keys.hide_action_bar_clear_history,REPORT_CHAT:Keys.hide_action_bar_report,REPORT_PROFILE:Keys.hide_action_bar_report,GIFT_PREMIUM:Keys.hide_action_bar_send_gift,CHANNEL_STORIES:Keys.hide_action_bar_archived_stories,ADD_SHORTCUT_PROFILE:Keys.hide_action_bar_add_shortcut,ADD_SHORTCUT_CHAT:Keys.hide_action_bar_add_shortcut,BOOST_GROUP:Keys.hide_action_bar_boost_group}
def _should_hide_menu_item(plugin,args):
	A=plugin
	if not args:return _B
	B=args[0]
	if not isinstance(B,int):return _B
	if B==15:
		if A.get_setting(Keys.hide_action_bar_clear_history,_B)or A.get_setting(Keys.hide_action_bar_live_stream,_B):return _C
	if B==24:
		if A.get_setting(Keys.hide_action_bar_report,_B)or A.get_setting(Keys.hide_action_bar_add_shortcut,_B):return _C
	if B in _ITEM_KEY_MAP:return bool(A.get_setting(_ITEM_KEY_MAP[B],_B))
	return _B
class ActionBarMenuItemAddSubItemHook(BaseHook):
	def after_hooked_method(C,param):
		A=param;B=A.getResult()
		if B is _A or not A.args:return
		if _should_hide_menu_item(C.plugin,A.args):B.setVisibility(View.GONE)
class ActionBarMenuItemLazilyAddSubItemHook(BaseHook):
	def after_hooked_method(C,param):
		A=param;B=A.getResult()
		if B is _A or not A.args:return
		if _should_hide_menu_item(C.plugin,A.args):B.setVisibility(View.GONE)
class ActionBarMenuItemSetSubItemShownHook(BaseHook):
	def before_hooked_method(B,param):
		A=param
		if not A.args:return
		if _should_hide_menu_item(B.plugin,A.args):
			try:A.args[1]=_B
			except IndexError:pass
class ChatActivityCheckActionBarMenuHook(BaseHook):
	def after_hooked_method(D,param):
		if not D.plugin.get_setting(Keys.hide_action_bar_clear_history,_B):return
		B=param.thisObject
		if B is not _A:
			try:
				A=get_private_field(B,'clearHistoryItem')
				if A is not _A:
					try:A.visibility=8
					except Exception:pass
					C=getattr(A,'view',_A)
					if C is not _A:C.setVisibility(8)
			except Exception:pass
class TopicsFragmentUpdateChatInfoHook(BaseHook):
	def after_hooked_method(A,param):
		B=param.thisObject
		if A.plugin.get_setting(Keys.hide_action_bar_boost_group,_B):
			C=get_private_field(B,'boostGroupSubmenu')
			if C is not _A:C.setVisibility(8)
		if A.plugin.get_setting(Keys.hide_action_bar_report,_B):
			D=get_private_field(B,'reportSubmenu')
			if D is not _A:D.setVisibility(8)
def register_action_bar(plugin):
	A=plugin;B=find_class('org.telegram.ui.ActionBar.ActionBarMenuItem');C=find_class(_J);D=find_class('org.telegram.ui.TopicsFragment')
	if B:
		try:A.hook_all_methods(B,'addSubItem',ActionBarMenuItemAddSubItemHook(A))
		except Exception:pass
		try:A.hook_all_methods(B,'lazilyAddSubItem',ActionBarMenuItemLazilyAddSubItemHook(A))
		except Exception:pass
		try:A.hook_all_methods(B,'setSubItemShown',ActionBarMenuItemSetSubItemShownHook(A))
		except Exception:pass
	if C:
		try:A.hook_all_methods(C,'checkActionBarMenu',ChatActivityCheckActionBarMenuHook(A))
		except Exception:pass
	if D:
		try:A.hook_all_methods(D,'updateChatInfo',TopicsFragmentUpdateChatInfoHook(A))
		except Exception:pass
BUTTON_GIFT=1
class ChatActivityEnterViewCreateGiftButtonHook(BaseHook):
	def before_hooked_method(A,param):
		if A.is_enabled():param.setResult(_A)
class ChatActivityEnterViewUpdateGiftButtonHook(BaseHook):
	def before_hooked_method(A,param):
		if A.is_enabled():param.setResult(_A)
class ChatActivityEnterViewAddGiftViewHook(BaseHook):
	def before_hooked_method(C,param):
		A=param
		if not C.is_enabled():return
		if not A.args:return
		try:
			B=A.args[0]
			if hasattr(B,'getContentDescription'):
				D=str(B.getContentDescription()or'').lower()
				if'gift'in D:A.setResult(_A)
		except Exception:pass
class ChatActivityShowGiftButtonHook(BaseHook):
	def before_hooked_method(A,param):
		if A.is_enabled():param.setResult(_A)
class ChatActivityChannelButtonsLayoutShowButtonHook(BaseHook):
	def before_hooked_method(B,param):
		A=param
		if B.is_enabled():
			try:
				if A.args[0]==BUTTON_GIFT:A.setResult(_A)
			except IndexError:pass
def register_gift_button(plugin):
	A=plugin;B=find_class(_Am)
	if B:
		try:A.hook_all_methods(B,'createGiftButton',ChatActivityEnterViewCreateGiftButtonHook(A,Keys.hide_bottom_gift_button))
		except Exception:pass
		try:A.hook_all_methods(B,'updateGiftButton',ChatActivityEnterViewUpdateGiftButtonHook(A,Keys.hide_bottom_gift_button))
		except Exception:pass
		try:A.hook_all_methods(B,'addView',ChatActivityEnterViewAddGiftViewHook(A,Keys.hide_bottom_gift_button))
		except Exception:pass
	C=find_class(_J)
	if C:
		try:A.hook_all_methods(C,'showGiftButton',ChatActivityShowGiftButtonHook(A,Keys.hide_bottom_gift_button))
		except Exception:pass
	D=find_class('org.telegram.ui.Components.chat.layouts.ChatActivityChannelButtonsLayout')
	if D:
		try:A.hook_all_methods(D,'showButton',ChatActivityChannelButtonsLayoutShowButtonHook(A,Keys.hide_bottom_gift_button))
		except Exception:pass
ALL_HIDDEN_TYPES=18,25,30,31,33,34
ALL_HIDDEN_ACTION_CLASSES='org.telegram.tgnet.TLRPC$TL_messageActionGift','org.telegram.tgnet.TLRPC$TL_messageActionGiftCode','org.telegram.tgnet.TLRPC$TL_messageActionGiftPremium','org.telegram.tgnet.TLRPC$TL_messageActionGiftStars','org.telegram.tgnet.TLRPC$TL_messageActionGiftTon','org.telegram.tgnet.TLRPC$TL_messageActionStarGift','org.telegram.tgnet.TLRPC$TL_messageActionStarGiftUnique','org.telegram.tgnet.TLRPC$TL_messageActionStarGiftPurchaseOffer','org.telegram.tgnet.TLRPC$TL_messageActionStarGiftPurchaseOfferDeclined'
ALL_HIDDEN_MEDIA_CLASSES='org.telegram.tgnet.TLRPC$TL_messageMediaGiftStars','org.telegram.tgnet.TLRPC$TL_messageMediaGiftPremium','org.telegram.tgnet.TLRPC$TL_messageMediaStarGift','org.telegram.tgnet.TLRPC$TL_messageMediaStarGiftUnique'
GIFT_OBJECT_CLASS_NAMES='org.telegram.tgnet.tl.TL_stars$TL_starGift',_A_
GIFT_OBJECT_PREFIXES='org.telegram.tgnet.tl.TL_stars$TL_starGiftUnique_',_A_
def _get_java_class_name(value):
	A=value
	try:return str(A.getClass().getName()or'')
	except Exception:
		try:return str(type(A)or'')
		except Exception:return''
def _has_gift_ids(obj):
	if obj is _A:return _B
	for A in('saved_id','gift_msg_id'):
		try:
			B=getattr(obj,A,0)or 0
			if int(B)!=0:return _C
		except Exception:pass
	return _B
def _has_gift_object_metadata(action_or_media):
	B=action_or_media
	if B is _A:return _B
	try:
		A=getattr(B,'gift',_A)
		if A is _A:return _B
		C=_get_java_class_name(A)
		if C in GIFT_OBJECT_CLASS_NAMES:return _C
		if any(C.startswith(A)for A in GIFT_OBJECT_PREFIXES):return _C
		D=getattr(A,'slug',_A)
		if D:return _C
	except Exception:pass
	return _B
def _is_giveaway(item):return'Giveaway'in _get_java_class_name(item)
def _is_gift(item):A=_get_java_class_name(item);return A in ALL_HIDDEN_ACTION_CLASSES or A in ALL_HIDDEN_MEDIA_CLASSES
def is_gift_message(msg):
	B=msg
	try:
		if B is _A:return _B
		try:
			D=int(getattr(B,_H,0)or 0)
			if D in ALL_HIDDEN_TYPES:return _C
		except Exception:pass
		C=getattr(B,_B0,_A)
		if C is _A:return _B
		E=getattr(C,'media',_A);F=getattr(C,'action',_A)
		for A in(E,F):
			if A is not _A:
				if _is_gift(A):return _C
				if _has_gift_object_metadata(A):return _C
				if _has_gift_ids(A):return _C
		return _B
	except Exception:return _B
def is_giveaway_message(msg):
	B=msg
	try:
		if B is _A:return _B
		try:
			D=int(getattr(B,_H,0)or 0)
			if D in(26,28):return _C
		except Exception:pass
		A=getattr(B,_B0,_A)
		if A is _A:return _B
		if getattr(A,'via_giveaway',_B):return _C
		E=getattr(A,'media',_A);F=getattr(A,'action',_A)
		for C in(E,F):
			if C is not _A:
				if _is_giveaway(C):return _C
		return _B
	except Exception:return _B
HIDDEN_VIEW_TYPE=-1000
View=find_class('android.view.View')
RecyclerViewLayoutParams=find_class('androidx.recyclerview.widget.RecyclerView$LayoutParams')
Holder=find_class('org.telegram.ui.Components.RecyclerListView$Holder')
class CollapseItemViewTypeHook(BaseHook):
	def before_hooked_method(C,param):
		A=param;D=C.plugin.get_setting(Keys.hide_gift_cards,_B);E=C.plugin.get_setting(Keys.hide_giveaway_cards,_B)
		if not D and not E:return
		if not A.args or len(A.args)<1:return
		try:
			F=A.thisObject;G=A.args[0];B=get_msg(F,_A,G)
			if B is _A:return
			if D and is_gift_message(B)or E and is_giveaway_message(B):A.setResult(jint(HIDDEN_VIEW_TYPE))
		except Exception:pass
class CollapseCreateHook(BaseHook):
	def before_hooked_method(C,param):
		A=param;D=C.plugin.get_setting(Keys.hide_gift_cards,_B);E=C.plugin.get_setting(Keys.hide_giveaway_cards,_B)
		if not D and not E:return
		if not A.args or len(A.args)<2:return
		F=A.args[1]
		if F!=HIDDEN_VIEW_TYPE:return
		try:
			G=A.args[0];H=G.getContext()
			if View and RecyclerViewLayoutParams and Holder:B=View(H);B.setLayoutParams(RecyclerViewLayoutParams(-1,0));B.setMinimumHeight(0);A.setResult(Holder(B))
		except Exception:pass
class CollapseBindHook(BaseHook):
	def before_hooked_method(C,param):
		A=param;D=C.plugin.get_setting(Keys.hide_gift_cards,_B);E=C.plugin.get_setting(Keys.hide_giveaway_cards,_B)
		if not D and not E:return
		if not A.args or len(A.args)<2:return
		try:
			G=A.thisObject;F=A.args[1]
			if not isinstance(F,int):return
			B=get_msg(G,_A,F)
			if B is _A:return
			if D and is_gift_message(B)or E and is_giveaway_message(B):A.setResult(_A)
		except Exception:pass
def get_msg(adapter,holder,position):
	E=position;B=adapter;C=getattr(B,'messagesStartRow',-1)
	if C<0 or E<C:return
	A=B.getMessages()if hasattr(B,'getMessages')else _A
	if A is _A:return
	D=E-C
	if D<0:return
	return A.get(D)if hasattr(A,'get')else A[D]
def register_gift_cards(plugin):
	A=plugin;B=find_class('org.telegram.ui.ChatActivity$ChatActivityAdapter')
	if B:
		try:A.hook_all_methods(B,'getItemViewType',CollapseItemViewTypeHook(A),priority=120)
		except Exception:pass
		try:A.hook_all_methods(B,_B1,CollapseCreateHook(A),priority=120)
		except Exception:pass
		try:A.hook_all_methods(B,_B2,CollapseBindHook(A),priority=120)
		except Exception:pass
class ChatActivityShowGreetInfoHook(BaseHook):
	def before_hooked_method(A,param):
		if A.is_enabled():param.args[0]=_B
def register_greeting_button(plugin):
	A=plugin;B=find_class(_J)
	if B:
		try:A.hook_all_methods(B,'showGreetInfo',ChatActivityShowGreetInfoHook(A,Keys.hide_greeting_button))
		except Exception:pass
class TranslateControllerIsSummarizableHook(BaseHook):
	def before_hooked_method(A,param):
		if A.plugin.get_setting(Keys.hide_ai_summarize,_B):param.setResult(_B)
class ChatActivityShowAiButtonHook(BaseHook):
	def before_hooked_method(B,param):
		A=param
		if B.plugin.get_setting(Keys.hide_ai_button,_B):
			try:
				if A.args:A.args[0]=Boolean(_B)
			except Exception:pass
def register_hide_ai_features(plugin):
	A=plugin
	try:
		B=find_class('org.telegram.messenger.TranslateController')
		if B is not _A:A.hook_all_methods(B,'isSummarizable',TranslateControllerIsSummarizableHook(A))
	except Exception:pass
	D=_Am,'org.telegram.ui.Components.ChatAttachAlert','org.telegram.ui.Components.CaptionPhotoViewer'
	for E in D:
		try:
			C=find_class(E)
			if C is not _A:A.hook_all_methods(C,'showAiButton',ChatActivityShowAiButtonHook(A))
		except Exception:pass
class MessagesControllerRichEditorAvailableHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin,Keys.hide_articles_editor)
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_B)
def register_hide_articles(plugin):
	A=plugin
	try:
		if parse_version(get_client_version())<(12,9,0):return
		B=find_class(_K)
		if B is not _A:A.hook_all_methods(B,'richEditorAvailable',MessagesControllerRichEditorAvailableHook(A))
	except Exception:pass
StickerCategoriesListView=find_class(_B3)
class UpdateCategoriesShownHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin,Keys.hide_emoji_search)
	def before_hooked_method(B,param):
		A=param
		if not B.is_enabled():return
		if A.args and len(A.args)>0:
			try:A.args[0]=_B
			except Exception:pass
class StickerCategoriesVisibilityHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin,Keys.hide_emoji_search)
	def after_hooked_method(B,param):
		if not B.is_enabled():return
		A=param.thisObject
		if A:
			try:
				if A.getVisibility()!=8:A.setVisibility(8)
			except Exception:pass
def register_hide_emoji_search(plugin):
	A=plugin
	if StickerCategoriesListView:
		try:A.hook_all_methods(StickerCategoriesListView,'updateCategoriesShown',UpdateCategoriesShownHook(A));A.hook_all_methods(StickerCategoriesListView,'onLayout',StickerCategoriesVisibilityHook(A))
		except Exception:pass
class HideGroupStickerSetHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin,Keys.hide_group_stickers)
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_A)
def register_hide_group_stickers(plugin):
	A=plugin
	try:
		B=find_class(_An)
		if B is not _A:A.hook_all_methods(B,'getGroupStickerSetById',HideGroupStickerSetHook(A))
	except Exception:pass
_fields_cache={}
_field_cache={}
def get_row_fields(clazz):
	C=clazz;B=C.getName()
	if B not in _fields_cache:
		D=[]
		try:
			E=C.getDeclaredFields()
			for A in E:
				try:
					if A.getType().getName()=='int'and'row'in A.getName().lower():
						if not A.getModifiers()&Modifier.STATIC:A.setAccessible(_C);D.append(A)
				except Exception:pass
		except Exception:pass
		_fields_cache[B]=D
	return _fields_cache[B]
def get_cached_field(clazz,field_name):
	C=field_name;B=clazz;E=B.getName();A=E,C
	if A not in _field_cache:
		try:D=B.getDeclaredField(C);D.setAccessible(_C);_field_cache[A]=D
		except Exception:_field_cache[A]=_A
	return _field_cache[A]
def hide_row(clazz,instance,field_name):
	B=clazz;A=instance
	try:
		C=get_cached_field(B,field_name)
		if C is _A:return
		E=C.getInt(A)
		if E!=-1:
			D=get_cached_field(B,_Ao)
			if D is _A:return
			F=D.getInt(A);I=get_row_fields(B);C.setInt(A,jint(-1))
			for G in I:
				H=G.getInt(A)
				if E<H<F:G.setInt(A,jint(H-1))
			D.setInt(A,jint(F-1))
	except Exception:pass
class PrivacySettingsActivityUpdateRowsHook(BaseHook):
	def after_hooked_method(I,param):
		if not I.plugin.get_setting(Keys.hide_premium_features,_B):return
		A=param.thisObject
		if not A:return
		try:
			B=A.getClass();C=get_cached_field(B,'voicesRow')
			if C is _A:return
			E=C.getInt(A)
			if E!=-1:
				D=get_cached_field(B,_Ao)
				if D is _A:return
				F=D.getInt(A);J=get_row_fields(B);C.setInt(A,jint(-1))
				for G in J:
					H=G.getInt(A)
					if E<H<F:G.setInt(A,jint(H-1))
				D.setInt(A,jint(F-1))
		except Exception:pass
class PrivacySettingsActivityAddPremiumStarHook(BaseHook):
	def before_hooked_method(C,param):
		B=param
		if not C.plugin.get_setting(Keys.hide_premium_features,_B):return
		A=B.args
		if A and A[0]:B.setResult(A[0])
class PrivacyControlActivitySetMessageTextHook(BaseHook):
	def before_hooked_method(D,param):
		if not D.plugin.get_setting(Keys.hide_premium_features,_B):return
		A=param.thisObject
		try:
			B=A.getClass();C=get_cached_field(B,'rulesType')
			if C is _A:return
			E=C.getInt(A)
			if E==10:hide_row(B,A,'payRow');hide_row(B,A,'priceButtonRow')
		except Exception:pass
class FiltersSetupActivityUpdateRowsHook(BaseHook):
	def after_hooked_method(J,param):
		if not J.plugin.get_setting(Keys.hide_premium_features,_B):return
		A=param.thisObject
		try:
			D=A.getClass();E=D.getDeclaredField('showTagsRow');E.setAccessible(_C);B=E.getInt(A)
			if B!=-1:
				F=D.getDeclaredField(_L);F.setAccessible(_C);C=F.get(A)
				if C and B<C.size():
					C.remove(int(B))
					if B<C.size():C.remove(int(B))
					E.setInt(A,jint(-1))
					try:G=D.getDeclaredField('folderTagsPosition');G.setAccessible(_C);G.setInt(A,jint(-1))
					except Exception:pass
					H=D.getDeclaredField('adapter');H.setAccessible(_C);I=H.get(A)
					if I:I.notifyDataSetChanged()
		except Exception:pass
def _get_item_view_type(item):
	A=item
	if A is _A:return-1
	try:
		C=getattr(A,_Ap,_A)
		if C is not _A:return int(C)
	except Exception:pass
	try:
		B=A.getClass()
		while B is not _A:
			try:D=B.getDeclaredField(_Ap);D.setAccessible(_C);return int(D.getInt(A))
			except Exception:pass
			try:B=B.getSuperclass()
			except Exception:break
	except Exception:pass
	return-1
def _get_item_text(item):
	E='text';B=item
	if B is _A:return''
	try:
		A=getattr(B,E,_A)
		if A is not _A:return str(A)
	except Exception:pass
	try:
		C=B.getClass()
		while C is not _A:
			try:
				D=C.getDeclaredField(E);D.setAccessible(_C);A=D.get(B)
				if A is not _A:return str(A)
			except Exception:pass
			try:C=C.getSuperclass()
			except Exception:break
	except Exception:pass
	return''
class FilterCreateActivityUpdateRowsHook(BaseHook):
	def after_hooked_method(T,param):
		if not T.plugin.get_setting(Keys.hide_premium_features,_B):return
		E=param.thisObject
		if not E:return
		try:
			F=E.getClass();G=_A;H=F
			while H is not _A:
				try:G=H.getDeclaredField(_L);G.setAccessible(_C);break
				except Exception:H=H.getSuperclass()
			if not G:return
			A=G.get(E)
			if not A:return
			K=9;L=10
			try:
				M=F.getDeclaredField('VIEW_TYPE_HEADER_COLOR_PREVIEW');M.setAccessible(_C);B=M.get(_A)
				if B is not _A:K=int(B)
			except Exception:pass
			try:
				N=F.getDeclaredField('VIEW_TYPE_COLOR');N.setAccessible(_C);B=N.get(_A)
				if B is not _A:L=int(B)
			except Exception:pass
			O=find_class('org.telegram.messenger.LocaleController');P=find_class('org.telegram.messenger.R');I=''
			if O is not _A and P is not _A:
				try:I=str(O.getString('FolderTagColorInfo',P.string.FolderTagColorInfo))
				except Exception:pass
			C=A.size()-1
			while C>=0:
				J=A.get(C)
				if J:
					Q=_get_item_view_type(J)
					if Q in(K,L):A.remove(C)
					elif Q in(0,3,6):
						D=_get_item_text(J)
						if D and(I and I in D or _B4 in D.lower()or _B5 in D.lower()or _B6 in D.lower()):A.remove(C)
				C-=1
			try:
				R=F.getDeclaredField('adapter');R.setAccessible(_C);S=R.get(E)
				if S:S.notifyDataSetChanged()
			except Exception:pass
		except Exception:pass
class ListAdapterOnCreateViewHolderHook(BaseHook):
	def after_hooked_method(E,param):
		if not E.plugin.get_setting(Keys.hide_premium_features,_B):return
		C=param.getResult()
		if C:
			try:
				A=C.itemView
				if A is not _A:
					D=A.getClass().getName()
					if _B7 in D or _B8 in D:
						A.setVisibility(8);B=A.getLayoutParams()
						if B is not _A:B.height=0;B.width=0;A.setLayoutParams(B)
			except Exception:pass
class ListAdapterOnBindViewHolderHook(BaseHook):
	def after_hooked_method(P,param):
		D=param
		if not P.plugin.get_setting(Keys.hide_premium_features,_B):return
		E=D.args[0]
		if not E:return
		try:
			B=E.itemView
			if B is not _A:
				H=B.getClass().getName();I=E.getItemViewType()
				if I in(9,10)or _B7 in H or _B8 in H:
					B.setVisibility(8);A=B.getLayoutParams()
					if A is not _A:A.height=0;A.width=0;B.setLayoutParams(A)
					return
				if I in(0,3,6):
					J=D.thisObject;K=J.getClass().getDeclaredField('this$0');K.setAccessible(_C);F=K.get(J)
					if F is not _A:
						L=F.getClass().getDeclaredField(_L);L.setAccessible(_C);G=L.get(F);M=D.args[1]
						if G and M<G.size():
							N=G.get(M)
							if N:
								O=_get_item_text(N);C=O.lower()if O else''
								if C and(_B4 in C or _B5 in C or _B6 in C):
									B.setVisibility(8);A=B.getLayoutParams()
									if A is not _A:A.height=0;A.width=0;B.setLayoutParams(A)
		except Exception:pass
class MessagesControllerIsTranslationsAutoEnabledHook(BaseHook):
	def before_hooked_method(A,param):
		if A.plugin.get_setting(Keys.hide_premium_features,_B):param.setResult(_B)
class SettingsRegistryCreateEntriesHook(BaseHook):
	def after_hooked_method(A,param):
		if A.plugin.get_setting(Keys.hide_premium_features,_B):remove_extera_setting_entry(param.thisObject,_B9)
class MessagesControllerConstructorHook(BaseHook):
	def after_hooked_method(C,param):
		if C.plugin.get_setting(Keys.hide_premium_features,_B):
			A=param.thisObject
			if A:
				try:B=A.getClass().getDeclaredField(_M);B.setAccessible(_C);B.setBoolean(A,_B)
				except Exception:pass
class MessagesControllerSetFolderTagsHook(BaseHook):
	def before_hooked_method(D,param):
		B=param
		if D.plugin.get_setting(Keys.hide_premium_features,_B):
			try:B.args[0]=_B
			except Exception:pass
			A=B.thisObject
			if A:
				try:C=A.getClass().getDeclaredField(_M);C.setAccessible(_C);C.setBoolean(A,_B)
				except Exception:pass
class DialogCellTagsUpdateHook(BaseHook):
	def before_hooked_method(D,param):
		A=param
		if not D.plugin.get_setting(Keys.hide_premium_features,_B):return
		B=A.thisObject
		if B:
			try:
				C=getattr(B,'tags',_A)
				if C:C.clear()
			except Exception:pass
		A.setResult(_B)
class FilterCreateActivityConstructorHook(BaseHook):
	def after_hooked_method(A,param):A.plugin.in_filter_create=_C
class FilterCreateActivityOnFragmentDestroyHook(BaseHook):
	def before_hooked_method(A,param):A.plugin.in_filter_create=_B
class UserConfigIsPremiumHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.plugin.get_setting(Keys.hide_premium_features,_B):return
		if getattr(A.plugin,'in_filter_create',_B):param.setResult(_C)
class GeneralPreferencesActivityFillItemsHook(BaseHook):
	def after_hooked_method(E,param):
		if E.plugin.get_setting(Keys.hide_premium_features,_B):
			B=param.args[0]
			if B:
				C=B.size()-1
				while C>=0:
					A=B.get(C)
					if A:
						try:
							D=-1
							try:D=A.getClass().getField(_N).getInt(A)
							except Exception:
								try:D=A.getClass().getField('f1708id').getInt(A)
								except Exception:pass
							if D==2:B.remove(C)
						except Exception:pass
					C-=1
def is_matching_entry(entry,alias_to_remove):
	A=entry
	if not A:return _B
	try:
		C=A.getClass();D=C.getDeclaredFields()
		for B in D:
			if B.getName()=='guid':
				B.setAccessible(_C);E=B.get(A)
				if E==alias_to_remove:return _C
	except Exception:pass
	return _B
def remove_extera_setting_entry(registry,alias_to_remove):
	L='List';E=alias_to_remove;D=registry
	try:
		G=D.getClass().getDeclaredField('entriesStringAlias');G.setAccessible(_C);H=G.get(D)
		if H:H.remove(E)
	except Exception:pass
	try:
		I=D.getClass().getDeclaredField('preparedEntries');I.setAccessible(_C);B=I.get(D)
		if B:
			M=B.getClass();J=M.getName()
			if'Map'in J:
				N=list(B.keySet().toArray())
				for K in N:
					C=B.get(K)
					if C:
						O=C.getClass();P=O.getName()
						if L in P:
							A=C.size()-1
							while A>=0:
								F=C.get(A)
								if is_matching_entry(F,E):C.remove(A)
								A-=1
						elif is_matching_entry(C,E):B.remove(K)
			elif L in J:
				A=B.size()-1
				while A>=0:
					F=B.get(A)
					if is_matching_entry(F,E):B.remove(A)
					A-=1
	except Exception:pass
class TextInfoPrivacyCellSetTextHook(BaseHook):
	def before_hooked_method(E,param):
		B=param
		if not E.plugin.get_setting(Keys.hide_premium_features,_B):return
		C=B.args[0]
		if C is not _A:
			A=str(C)
			if'Вы можете запретить входящие сообщения'in A or'You can restrict who can send you messages'in A:
				if'\n'in A:A=A.split('\n')[0].strip()
				elif'**'in A:A=A.split('**')[0].strip()
				elif'Premium'in A:
					D=A.split('.')
					if len(D)>1:A=D[0].strip()+'.'
				B.args[0]=A
def register_hide_premium_features(plugin):
	A=plugin
	try:
		D=find_class('org.telegram.ui.PrivacySettingsActivity')
		if D:A.hook_all_methods(D,_O,PrivacySettingsActivityUpdateRowsHook(A));A.hook_all_methods(D,'addPremiumStar',PrivacySettingsActivityAddPremiumStarHook(A))
	except Exception:pass
	try:
		H=find_class('org.telegram.ui.PrivacyControlActivity')
		if H:A.hook_all_methods(H,'setMessageText',PrivacyControlActivitySetMessageTextHook(A))
	except Exception:pass
	try:
		I=find_class('org.telegram.ui.Cells.TextInfoPrivacyCell')
		if I:A.hook_all_methods(I,'setText',TextInfoPrivacyCellSetTextHook(A))
	except Exception:pass
	try:
		J=find_class('org.telegram.ui.FiltersSetupActivity')
		if J:A.hook_all_methods(J,_O,FiltersSetupActivityUpdateRowsHook(A))
	except Exception:pass
	try:
		B=find_class(_K)
		if B:
			A.hook_all_methods(B,'isTranslationsAutoEnabled',MessagesControllerIsTranslationsAutoEnabledHook(A));A.hook_all_constructors(B,MessagesControllerConstructorHook(A));A.hook_all_methods(B,'setFolderTags',MessagesControllerSetFolderTagsHook(A))
			if A.get_setting(Keys.hide_premium_features,_B):
				for P in range(4):
					try:
						E=B.getInstance(P)
						if E:K=E.getClass().getDeclaredField(_M);K.setAccessible(_C);K.setBoolean(E,_B)
					except Exception:pass
	except Exception:pass
	try:
		L=find_class('org.telegram.messenger.UserConfig')
		if L:A.hook_all_methods(L,'isPremium',UserConfigIsPremiumHook(A))
	except Exception:pass
	try:
		M=find_class('com.exteragram.messenger.preferences.GeneralPreferencesActivity')
		if M:A.hook_all_methods(M,_Aq,GeneralPreferencesActivityFillItemsHook(A))
	except Exception:pass
	try:
		F=find_class(_Az)
		if F:
			A.hook_all_methods(F,'createEntriesIfNeeded',SettingsRegistryCreateEntriesHook(A));N=F.getInstance()
			if N:remove_extera_setting_entry(N,_B9)
	except Exception:pass
	try:
		C=find_class('org.telegram.ui.FilterCreateActivity')
		if C:A.hook_all_methods(C,_O,FilterCreateActivityUpdateRowsHook(A));A.hook_all_constructors(C,FilterCreateActivityConstructorHook(A));A.hook_all_methods(C,'onFragmentDestroy',FilterCreateActivityOnFragmentDestroyHook(A))
		G=find_class('org.telegram.ui.FilterCreateActivity$ListAdapter')
		if G:A.hook_all_methods(G,_B1,ListAdapterOnCreateViewHolderHook(A));A.hook_all_methods(G,_B2,ListAdapterOnBindViewHolderHook(A))
	except Exception:pass
	try:
		O=find_class('org.telegram.ui.Components.DialogCellTags')
		if O:A.hook_all_methods(O,'update',DialogCellTagsUpdateHook(A))
	except Exception:pass
TL_profileTabGifts=find_class('org.telegram.tgnet.TLRPC$TL_profileTabGifts')
TL_profileTabPosts=find_class('org.telegram.tgnet.TLRPC$TL_profileTabPosts')
class SharedMediaLayoutHook(BaseHook):
	def __init__(A,plugin,is_constructor):super().__init__(plugin);A.is_constructor=is_constructor
	def _get_info_objects(C,param):
		A=param
		if C.is_constructor:
			try:return A.args[5],A.args[6]
			except IndexError:return _A,_A
		else:B=A.thisObject;D=get_private_field(B,'info');E=get_private_field(B,_Ar);return D,E
	def before_hooked_method(A,param):
		B=bool(A.plugin.get_setting(Keys.hide_gifts_tab,_B));C=bool(A.plugin.get_setting(Keys.hide_stories_tab,_B))
		if not B and not C:return
		E,F=A._get_info_objects(param)
		for D in[E,F]:
			if B:remove_gifts(D)
			if C:remove_stories(D)
	def after_hooked_method(A,param):
		B=bool(A.plugin.get_setting(Keys.hide_stories_tab,_B))
		if not B:return
		try:rebuild_tabs_without_stories(get_private_field(param.thisObject,'scrollSlidingTextTabStrip'))
		except Exception:pass
class SharedMediaLayoutSetInfoHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		try:B=param.args[0]
		except IndexError:return
		remove_stories(B)
class ReturnFalseHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_B)
class ProfileStoriesCollectionTabsSetVisibilityHook(BaseHook):
	def before_hooked_method(B,param):
		A=param
		if not B.is_enabled():return
		try:
			if A.args[0]:A.args[0]=_B
		except IndexError:pass
def remove_gifts(obj):
	A=obj
	if A:
		set_private_field(A,'stargifts_count',jint(0));B=get_private_field(A,_P)
		if TL_profileTabGifts and isinstance(B,TL_profileTabGifts):set_private_field(A,_P,_A)
def remove_stories(obj):
	A=obj
	if A:
		set_private_field(A,'stories_pinned_available',_B);set_private_field(A,'stories',_A);B=get_private_field(A,_P)
		if TL_profileTabPosts and isinstance(B,TL_profileTabPosts):set_private_field(A,_P,_A)
def rebuild_tabs_without_stories(tab_strip):
	A=tab_strip
	if A is _A:return
	if not A.hasTab(8)and not A.hasTab(9)and not A.hasTab(13):return
	D=A.getCurrentTabId();E={8,9,13};H=list(A.getTabIds());F=A.removeTabs();B=_A
	for C in H:
		if C in E:continue
		I=F.get(C);G=''
		try:G=I.getText()
		except Exception:pass
		A.addTextTab(C,G,F)
		if B is _A:B=C
	A.finishAddingTabs()
	if B is _A:return
	if D in E:A.scrollTo(B)
	else:A.selectTabWithId(D,1.)
def register_media_layout(plugin):
	A=plugin;B=find_class('org.telegram.ui.Components.SharedMediaLayout')
	if B:
		try:F=SharedMediaLayoutHook(A,is_constructor=_C);A.hook_all_constructors(B,F)
		except Exception:pass
		try:G=SharedMediaLayoutHook(A,is_constructor=_B);A.hook_all_methods(B,'updateTabs',G)
		except Exception:pass
		try:D=SharedMediaLayoutSetInfoHook(A,Keys.hide_stories_tab);A.hook_all_methods(B,'setChatInfo',D);A.hook_all_methods(B,'setUserInfo',D)
		except Exception:pass
		try:C=ReturnFalseHook(A,Keys.hide_stories_tab);A.hook_all_methods(B,'includeStories',C);A.hook_all_methods(B,'isStoriesView',C);A.hook_all_methods(B,'isArchivedOnlyStoriesView',C)
		except Exception:pass
	E=find_class('org.telegram.ui.ProfileStoriesCollectionTabs')
	if E:
		try:H=ProfileStoriesCollectionTabsSetVisibilityHook(A,Keys.hide_stories_tab);A.hook_all_methods(E,'setVisibility',H)
		except Exception:pass
class ProfileCollectibleHintBlockHook(BaseHook):
	def before_hooked_method(E,param):
		D='collectibleHint';B=param
		if not E.is_enabled():return
		try:
			A=B.thisObject;C=getattr(A,D,_A)
			if C:
				try:C.hide()
				except Exception:pass
			if hasattr(A,D):A.collectibleHint=_A
			if hasattr(A,'collectibleHintVisible'):A.collectibleHintVisible=_B
			if hasattr(A,'collectibleStatus'):A.collectibleStatus=_A
			if hasattr(A,'emojiStatusGiftId'):A.emojiStatusGiftId=_A
		except Exception:pass
		B.setResult(_A)
class ProfileEmojiStatusDrawableHook(BaseHook):
	def before_hooked_method(A,param):
		if A.plugin.get_setting(Keys.hide_premium_badge,_B):param.setResult(_A)
	def after_hooked_method(B,param):
		if not B.is_enabled():return
		try:
			A=param.getResult()
			if A and hasattr(A,'setParticles'):A.setParticles(_B,_C)
		except Exception:pass
class SmartEmojiStatusHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_A)
class ZeroHook(BaseHook):
	def before_hooked_method(A,param):param.setResult(0)
class MessagesControllerIsPremiumUserHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_B)
class DrawerMenuHidePremiumHook(BaseHook):
	def after_hooked_method(C,param):
		if not C.is_enabled():return
		try:
			B=param.thisObject
			if hasattr(B,'premiumStatusDrawable'):
				A=B.premiumStatusDrawable
				if A and hasattr(A,_E):A.set(_A,_B)
		except Exception:pass
_premium_gradient_class=_A
_status_badge_component_class=_A
_badge_dto_class=_A
_tlrpc_user_class=_A
_badges_controller_class=_A
def find_premium_gradient_class():
	global _premium_gradient_class
	if _premium_gradient_class is not _A:return _premium_gradient_class
	for B in['org.telegram.ui.Components.Premium.PremiumGradient','org.telegram.p035ui.Components.Premium.PremiumGradient','org.telegram.p022ui.Components.Premium.PremiumGradient','org.telegram.p031ui.Components.Premium.PremiumGradient']:
		try:
			A=find_class(B)
			if A:_premium_gradient_class=A;return A
		except Exception:pass
def find_status_badge_component_class():
	global _status_badge_component_class
	if _status_badge_component_class is not _A:return _status_badge_component_class
	for B in['org.telegram.ui.Components.StatusBadgeComponent','org.telegram.p035ui.Components.StatusBadgeComponent','org.telegram.p022ui.Components.StatusBadgeComponent','org.telegram.p031ui.Components.StatusBadgeComponent']:
		try:
			A=find_class(B)
			if A:_status_badge_component_class=A;return A
		except Exception:pass
class ChatMessageCellGetAuthorStatusHook(BaseHook):
	def after_hooked_method(D,param):
		C=param
		if not D.is_enabled():return
		A=C.getResult()
		if A is not _A:
			if isinstance(A,(int,float)):C.setResult(_A);return
			if hasattr(A,'getClass'):
				try:
					global _badge_dto_class
					if _badge_dto_class is _A:_badge_dto_class=find_class('com.exteragram.messenger.api.dto.BadgeDTO')
					if _badge_dto_class and _badge_dto_class.isInstance(A):return
				except Exception:pass
				try:
					B=A.getClass().getName()
					if'Drawable'in B or'Long'in B or'Integer'in B or'Number'in B:C.setResult(_A)
				except Exception:pass
class SwapAnimatedEmojiDrawableSetHook(BaseHook):
	def before_hooked_method(H,param):
		G='getConstantState';B=param
		if not H.is_enabled():return
		A=B.args[0]if B.args else _A
		if A is not _A and hasattr(A,'getClass'):
			try:
				C=A.getClass().getName()
				if'Drawable'in C:
					if'Premium'in C or'Star'in C or'liststar'in C.lower():B.args[0]=_A;return
					E=find_premium_gradient_class()
					if E:
						F=E.getInstance()
						if F:
							D=F.premiumStarDrawableMini
							if A==D or hasattr(A,G)and hasattr(D,G)and A.getConstantState()==D.getConstantState():B.args[0]=_A;return
			except Exception:pass
class StatusBadgeComponentUpdateDrawableHook(BaseHook):
	def after_hooked_method(F,param):
		D=param
		if not F.is_enabled():return
		global _tlrpc_user_class
		if _tlrpc_user_class is _A:
			try:_tlrpc_user_class=find_class('org.telegram.tgnet.TLRPC$User')
			except Exception:pass
		if _tlrpc_user_class is _A:return
		A=_A
		for B in D.args:
			if B is not _A and _tlrpc_user_class.isInstance(B):A=B;break
		if A:
			try:
				if hasattr(A,'verified')and A.verified:return
				global _badges_controller_class
				if _badges_controller_class is _A:_badges_controller_class=find_class('com.exteragram.messenger.badges.BadgesController')
				if _badges_controller_class:
					C=_B
					try:
						if _badges_controller_class.INSTANCE.getBadge(A)is not _A:C=_C
					except Exception:pass
					try:
						if _badges_controller_class.INSTANCE.hasBadge(A):C=_C
					except Exception:pass
					if C:return
			except Exception:pass
			E=D.getResult()
			if E is not _A:
				try:E.set(_A,_B)
				except Exception:pass
class FalseMethodReplacement(MethodReplacement):
	def replace_hooked_method(A,param):return _B
class NullMethodReplacement(MethodReplacement):
	def replace_hooked_method(A,param):0
def register_premium_badge(plugin):
	B='getEmojiStatusDocumentId';A=plugin
	if A.get_setting(Keys.hide_premium_badge,_B):
		try:
			O=SmartEmojiStatusHook(A);C=ZeroHook(A);D=find_class(_Q)
			if D:A.hook_all_methods(D,B,O)
			E=find_class(_R)
			if E:A.hook_all_methods(E,B,C)
			F=find_class(_S)
			if F:A.hook_all_methods(F,B,C)
			G=find_class(_K)
			if G:P=MessagesControllerIsPremiumUserHook(A,Keys.hide_premium_badge);A.hook_all_methods(G,'isPremiumUser',P)
			H=find_class(_BA)
			if H:Q=ChatMessageCellGetAuthorStatusHook(A,Keys.hide_premium_badge);A.hook_all_methods(H,'getAuthorStatus',Q)
			I=find_class('org.telegram.ui.Components.AnimatedEmojiDrawable$SwapAnimatedEmojiDrawable')
			if I:R=SwapAnimatedEmojiDrawableSetHook(A,Keys.hide_premium_badge);A.hook_all_methods(I,_E,R)
			J=find_status_badge_component_class()
			if J:S=StatusBadgeComponentUpdateDrawableHook(A,Keys.hide_premium_badge);A.hook_all_methods(J,'updateDrawable',S)
			K=find_class('com.exteragram.messenger.drawer.DrawerHeaderView');L=find_class('com.exteragram.messenger.drawer.DrawerAccountPickerView$AccountRowView');M=DrawerMenuHidePremiumHook(A,Keys.hide_premium_badge)
			if K:A.hook_all_methods(K,'updateUserInfo',M)
			if L:A.hook_all_methods(L,'bind',M)
			T=FalseMethodReplacement()
			for U in[_Q,_R,_S]:
				try:
					N=find_class(U)
					if N:A.hook_all_methods(N,_BB,T)
				except Exception:pass
		except Exception:pass
	_hook_collectible_status(A)
def _hook_collectible_status(plugin):
	A=plugin
	if A.get_setting(Keys.hide_collectible_status,_B):
		try:
			D=FalseMethodReplacement()
			for E in[_Q,_R,_S]:
				try:
					B=find_class(E)
					if B:A.hook_all_methods(B,_BB,D)
				except Exception:pass
		except Exception:pass
	try:C=find_class(_T);A.hook_all_methods(C,'setCollectibleGiftStatus',ProfileCollectibleHintBlockHook(A,Keys.hide_collectible_status));A.hook_all_methods(C,'getEmojiStatusDrawable',ProfileEmojiStatusDrawableHook(A,Keys.hide_collectible_status))
	except Exception:pass
def safe_find_class(class_name):
	try:return find_class(class_name)
	except Exception:return
Emoji=safe_find_class('org.telegram.messenger.Emoji')
EmojiView=safe_find_class('org.telegram.ui.Components.EmojiView')
EmojiGridAdapter=safe_find_class('org.telegram.ui.Components.EmojiView$EmojiGridAdapter')
EmojiSearchAdapter=safe_find_class('org.telegram.ui.Components.EmojiView$EmojiSearchAdapter')
SuggestEmojiView=safe_find_class('org.telegram.ui.Components.SuggestEmojiView')
ArrayList=safe_find_class(_BC)
MessageObject=safe_find_class(_S)
StickerEmojiCell=safe_find_class('org.telegram.ui.Cells.StickerEmojiCell')
ChatActivityEnterView=safe_find_class(_Am)
MediaDataController=safe_find_class(_An)
EmojiSearchAdapterRunnable=safe_find_class('org.telegram.ui.Components.EmojiView$EmojiSearchAdapter$5')
EmojiTabsStrip=safe_find_class('org.telegram.ui.Components.EmojiTabsStrip')
SelectAnimatedEmojiDialog=safe_find_class('org.telegram.ui.SelectAnimatedEmojiDialog')
CustomEmojiReactionsWindow=safe_find_class('org.telegram.ui.Components.Reactions.CustomEmojiReactionsWindow')
ReactionsContainerLayout=safe_find_class(_BD)
StickerCategoriesListView=safe_find_class(_B3)
SearchBox=safe_find_class('org.telegram.ui.SelectAnimatedEmojiDialog$SearchBox')
Thread=safe_find_class('java.lang.Thread')
def _init(plugin):0
def _is_non_stock(item):
	A=item
	if isinstance(A,str):return A.startswith(_U)
	if MessageObject:
		try:return bool(MessageObject.isAnimatedEmoji(A))
		except Exception:pass
	return _B
def _is_premium_sticker(document):
	A=document
	if A is _A:return _B
	try:
		B=getattr(A,'video_thumbs',_A)
		if B:
			for C in range(B.size()):
				try:
					if getattr(B.get(C),_H,_A)=='f':return _C
				except Exception:pass
		return _B
	except Exception:pass
	if MessageObject:
		try:return bool(MessageObject.isPremiumSticker(A))
		except Exception:pass
	return _B
def _filter_list(container,*,sub=_A,drop_empty=_B,drop_non_stock=_B,is_sticker=_B):
	I=is_sticker;G=drop_non_stock;B=container
	if not B or not ArrayList:return 0
	C=0;A=B.size()-1
	while A>=0:
		J=B.get(A)
		if sub:
			D=getattr(J,sub,_A)
			if D is not _A:
				K=getattr(J,_E,_A);M=G and K and getattr(K,'emojis',_B)
				if M:B.remove(A);C+=1;A-=1;continue
				E=D.size()-1
				while E>=0:
					L=D.get(E)
					if I:
						if _is_premium_sticker(L):D.remove(E);C+=1
					else:
						F=getattr(L,_V,_A)
						if F is not _A and bool(F):D.remove(E);C+=1
					E-=1
				if drop_empty and D.size()==0:B.remove(A);C+=1
		else:
			H=B.get(A)
			if G and _is_non_stock(H):B.remove(A);C+=1
			elif I:
				if _is_premium_sticker(H):B.remove(A);C+=1
			elif not G:
				F=getattr(H,_V,_A)
				if F is not _A and bool(F):B.remove(A);C+=1
		A-=1
	return C
def _filter_reactions_list(container):
	A=container
	if not A:return 0
	C=0;B=A.size()-1
	while B>=0:
		D=A.get(B);E=getattr(D,'documentId',0)or getattr(D,'document_id',0)
		if E!=0:A.remove(B);C+=1
		B-=1
	return C
def _is_featured_premium(pack):
	A=pack
	if not A or not MessageObject:return _B
	try:return bool(MessageObject.isPremiumEmojiPack(A))
	except Exception:pass
	for C in(_As,'covers'):
		B=getattr(A,C,_A)
		if B:
			for D in range(B.size()):
				try:
					if getattr(B.get(D),_V,_B):return _C
				except Exception:pass
	return _B
def _is_featured_non_stock(pack):
	if not pack:return _B
	A=getattr(pack,_E,_A)
	if A:
		try:return bool(A.emojis)
		except Exception:pass
	return _B
def _filter_featured_sets(sets):
	A=sets
	if not A:return 0
	C=0;B=A.size()-1
	while B>=0:
		D=A.get(B)
		if _is_featured_premium(D)or _is_featured_non_stock(D):A.remove(B);C+=1
		B-=1
	return C
def _is_premium_sticker_pack(pack):
	if not pack:return _B
	A=getattr(pack,'cover',_A)
	if A and _is_premium_sticker(A):return _C
	return _B
def _filter_featured_sticker_sets(sets):
	A=sets
	if not A:return 0
	C=0;B=A.size()-1
	while B>=0:
		if _is_premium_sticker_pack(A.get(B)):A.remove(B);C+=1
		B-=1
	return C
def _filter_search_results(results):
	B=results
	if not B:return 0
	C=0;A=B.size()-1
	while A>=0:
		D=B.get(A);E=getattr(D,'emoji',_A)
		if E is _A:
			try:E=str(D)
			except Exception:A-=1;continue
		if _is_non_stock(E):B.remove(A);C+=1;A-=1;continue
		try:
			if getattr(D,_V,_B):B.remove(A);C+=1
		except Exception:pass
		A-=1
	return C
def _reindex_search_sets(sets):
	B=sets
	if not B or not ArrayList:return
	D=ArrayList();A=ArrayList();F=_B
	for G in range(B.size()):
		C=B.get(G)
		if C and getattr(C,'title',_A)is not _A:
			if A.size()>0:
				D.add(C)
				for E in range(A.size()):D.add(A.get(E))
			A=ArrayList();continue
		if _is_non_stock(C):F=_C
		else:A.add(C)
	for E in range(A.size()):D.add(A.get(E))
	if F:B.clear();B.addAll(D)
def _filter_search_packs(packs_list):
	A=packs_list
	if not A:return 0
	C=0;B=A.size()-1
	while B>=0:
		E=A.get(B);D=getattr(E,_As,_A)
		if D:
			_filter_list(D,drop_non_stock=_C)
			if D.size()==0:A.remove(B);C+=1
		else:A.remove(B);C+=1
		B-=1
	return C
HANDLERS={_BE:lambda r,p:_filter_featured_sticker_sets(getattr(r,'sets',_A))if p.get_setting(Keys.hide_premium_stickers_grid,_B)else _A,_BF:lambda r,p:_filter_list(getattr(r,_At,_A),is_sticker=_C)if p.get_setting(Keys.hide_premium_stickers_search,_B)else _A,_BG:lambda r,p:_filter_list(getattr(r,_At,_A),is_sticker=_C)if p.get_setting(Keys.hide_premium_stickers_recent,_B)else _A,_BH:lambda r,p:_filter_list(getattr(r,_At,_A),is_sticker=_C)if p.get_setting(Keys.hide_premium_stickers_search,_B)else _A,_BI:lambda r,p:_filter_list(getattr(r,_As,_A),is_sticker=_C)if p.get_setting(Keys.hide_premium_stickers_search,_B)else _A}
def filter_response(request_name,response):
	A=HANDLERS.get(request_name)
	if A is _A:return
	try:B=LiteGramPlugin.get_instance();A(response,B)
	except Exception:pass
class BlockNonStockHook(BaseHook):
	def before_hooked_method(C,param):
		A=param
		if not C.is_enabled():return
		if not A.args:return
		B=A.args[0]
		if isinstance(B,str)and B.startswith(_U):A.setResult(_A)
class FilterRecentEmojiHook(BaseHook):
	def after_hooked_method(H,param):
		E=param
		if not H.is_enabled():return
		B=E.getResult()
		if not B or not ArrayList:return
		F=B.size();G=_B
		for C in range(F):
			A=B.get(C)
			if isinstance(A,str)and A.startswith(_U):G=_C;break
		if not G:return
		D=ArrayList()
		for C in range(F):
			A=B.get(C)
			if isinstance(A,str):
				if not A.startswith(_U):D.add(A)
			else:D.add(A)
		E.setResult(D)
class FilterSearchResultsHook(BaseHook):
	def before_hooked_method(B,param):
		A=param
		if not B.is_enabled():return
		if not A.args or len(A.args)<3:return
		_filter_search_results(A.args[1]);_reindex_search_sets(A.args[2])
class FilterSuggestResultsHook(BaseHook):
	def __init__(A,plugin,arg_index,label):super().__init__(plugin,Keys.hide_premium_suggestions);A._arg_index=arg_index;A._label=label
	def before_hooked_method(C,param):
		D=param
		if not C.is_enabled():return
		if not D.args or len(D.args)<=C._arg_index:return
		B=D.args[C._arg_index]
		if not B:return
		A=B.size()-1
		while A>=0:
			F=B.get(A);E=getattr(F,'emoji',_A)
			if E is _A:
				try:E=str(F)
				except Exception:A-=1;continue
			if _is_non_stock(E):B.remove(A)
			A-=1
def _hide_view(view):
	A=view
	if A is _A:return
	try:
		if A.getVisibility()!=8:A.setVisibility(8)
	except Exception:pass
def _restore_view(view):
	A=view
	if A is _A:return
	try:
		if A.getVisibility()!=0:A.setVisibility(0)
	except Exception:pass
class CheckDocumentsHook(BaseHook):
	def after_hooked_method(C,param):
		if not C.is_enabled():return
		D=param.thisObject
		for E in('favouriteStickers','recentStickers'):
			A=get_private_field(D,E)
			if not A:continue
			B=A.size()-1
			while B>=0:
				if _is_premium_sticker(A.get(B)):A.remove(B)
				B-=1
class HidePremiumStickerCellHook(BaseHook):
	def before_hooked_method(C,param):
		A=param
		if not C.is_enabled():return
		if not A.args:return
		D=A.args[0];B=A.thisObject
		if _is_premium_sticker(D):_hide_view(B);A.setResult(_A)
		else:_restore_view(B)
class SkipEmojiPacksHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin,Keys.hide_premium_emoji_keyboard)
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_A)
class FilterEmojiPacksHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin,Keys.hide_premium_emoji_keyboard)
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		if ArrayList:param.setResult(ArrayList())
class FilterSearchV7Hook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin,Keys.hide_premium_search)
	def before_hooked_method(B,param):
		A=param
		if not B.is_enabled():return
		if not A.args or len(A.args)<3:return
		_filter_search_results(A.args[1]);_reindex_search_sets(A.args[2])
class FilterSearchV12_8_1Hook(BaseHook):
	def before_hooked_method(D,param):
		A=param
		if not D.is_enabled():return
		if not A.args or len(A.args)<5:return
		E=A.thisObject;B=getattr(E,'this$0',_A)
		if B:
			C=get_private_field(B,'resultPre')
			if C:_filter_search_results(C)
		_filter_search_results(A.args[1]);_filter_search_packs(A.args[2]);_filter_search_packs(A.args[3])
class BlockGlobalSearchHook(BaseHook):
	def before_hooked_method(C,param):
		A=param
		if not C.is_enabled():return
		if not A.args:return
		D=A.method.getName()
		if D==_BJ:B=A.args[0]
		else:B=A.args[-1]
		if B:B.run()
		A.setResult(_A)
class EmojiTabsStripConstructorHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin,Keys.hide_premium_emoji_keyboard)
	def before_hooked_method(B,param):
		A=param
		if not B.is_enabled():return
		if not A.args or len(A.args)<6:return
		if not A.args[4]:return
		if len(A.args)>6 and isinstance(A.args[6],int):
			if A.args[6]!=0:return
		try:A.args[5]=_B
		except Exception:pass
class FilterReactionsLayoutHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin,Keys.hide_premium_emoji_reactions)
	def before_hooked_method(C,param):
		if not C.is_enabled():return
		A=param.thisObject
		if not A:return
		try:
			B=get_private_field(A,'allReactionsList')
			if B:_filter_reactions_list(B)
		except Exception:pass
_in_reactions_dialog_update=_B
class FilterStickerSetsType5Hook(BaseHook):
	def after_hooked_method(A,param):
		global _in_reactions_dialog_update
		if _in_reactions_dialog_update:
			if ArrayList:param.setResult(ArrayList())
class FilterFeaturedEmojiSetsHook(BaseHook):
	def after_hooked_method(A,param):
		global _in_reactions_dialog_update
		if _in_reactions_dialog_update:
			if ArrayList:param.setResult(ArrayList())
class SelectAnimatedEmojiDialogUpdateRowsHook(BaseHook):
	def before_hooked_method(D,param):
		global _in_reactions_dialog_update;A=param.thisObject
		if not A:return
		try:
			E=get_private_field(A,_H);F=D.plugin
			if E in(1,8,11)and F.get_setting(Keys.hide_premium_emoji_reactions,_B):
				_in_reactions_dialog_update=_C;B=get_private_field(A,'recentReactionsToSet')
				if B:_filter_reactions_list(B)
				C=get_private_field(A,'frozenEmojiPacks')
				if C is not _A:
					try:C.clear()
					except Exception:pass
				elif ArrayList:
					try:A.frozenEmojiPacks=ArrayList()
					except Exception:pass
		except Exception:pass
	def after_hooked_method(E,param):
		global _in_reactions_dialog_update;_in_reactions_dialog_update=_B;A=param.thisObject
		if not A:return
		try:
			F=get_private_field(A,_H);G=E.plugin
			if F in(1,8,11)and G.get_setting(Keys.hide_premium_emoji_reactions,_B):
				B=get_private_field(A,'emojiTabs')
				if B:_hide_view(B)
				C=get_private_field(A,'emojiTabsShadow')
				if C:_hide_view(C)
				D=get_private_field(A,'packs')
				if D:
					try:D.clear()
					except Exception:pass
		except Exception:pass
def register_premium_emoji(plugin):
	A=plugin;_init(A);B=[]
	if SelectAnimatedEmojiDialog:
		try:A.hook_all_methods(SelectAnimatedEmojiDialog,_O,SelectAnimatedEmojiDialogUpdateRowsHook(A));B.append('SelectAnimatedEmojiDialog')
		except Exception:pass
	if ReactionsContainerLayout:
		try:C=FilterReactionsLayoutHook(A);A.hook_all_methods(ReactionsContainerLayout,'showCustomEmojiReactionDialog',C);A.hook_all_constructors(ReactionsContainerLayout,C);B.append('ReactionsContainerLayout')
		except Exception:pass
	if Emoji:
		try:A.hook_all_methods(Emoji,'addRecentEmoji',BlockNonStockHook(A,Keys.hide_premium_emoji_keyboard));B.append('Emoji')
		except Exception:pass
	if EmojiTabsStrip:
		try:A.hook_all_constructors(EmojiTabsStrip,EmojiTabsStripConstructorHook(A));B.append('EmojiTabsStrip')
		except Exception:pass
	if EmojiView:
		try:A.hook_all_methods(EmojiView,'getRecentEmoji',FilterRecentEmojiHook(A,Keys.hide_premium_emoji_keyboard))
		except Exception:pass
		try:A.hook_all_methods(EmojiView,'checkDocuments',CheckDocumentsHook(A,Keys.hide_premium_stickers_recent))
		except Exception:pass
		try:A.hook_all_methods(EmojiView,'getEmojipacks',FilterEmojiPacksHook(A))
		except Exception:pass
		B.append('EmojiView')
	if EmojiSearchAdapter:
		try:A.hook_all_methods(EmojiSearchAdapter,'lambda$search$5',FilterSearchResultsHook(A,Keys.hide_premium_search))
		except Exception:pass
		try:A.hook_all_methods(EmojiSearchAdapter,'lambda$search$7',FilterSearchV7Hook(A))
		except Exception:pass
		try:A.hook_all_methods(EmojiSearchAdapter,'lambda$search$3',BlockGlobalSearchHook(A,Keys.hide_premium_search))
		except Exception:pass
		try:A.hook_all_methods(EmojiSearchAdapter,_BJ,BlockGlobalSearchHook(A,Keys.hide_premium_search))
		except Exception:pass
		B.append('EmojiSearchAdapter')
	if EmojiSearchAdapterRunnable:
		try:A.hook_all_methods(EmojiSearchAdapterRunnable,'lambda$run$7',FilterSearchV12_8_1Hook(A,Keys.hide_premium_search));B.append('EmojiSearchAdapterRunnable')
		except Exception:pass
	if SuggestEmojiView:
		try:A.hook_all_methods(SuggestEmojiView,'lambda$searchKeywords$3',FilterSuggestResultsHook(A,4,'keywords'))
		except Exception:pass
		try:A.hook_all_methods(SuggestEmojiView,'lambda$searchAnimated$5',FilterSuggestResultsHook(A,2,'animated'))
		except Exception:pass
		B.append('SuggestEmojiView')
	if StickerEmojiCell:
		try:A.hook_all_methods(StickerEmojiCell,'setSticker',HidePremiumStickerCellHook(A,Keys.hide_premium_stickers_grid));B.append('StickerEmojiCell')
		except Exception:pass
	if MediaDataController:
		try:A.hook_all_methods(MediaDataController,'getStickerSets',FilterStickerSetsType5Hook(A))
		except Exception:pass
		try:A.hook_all_methods(MediaDataController,'getFeaturedEmojiSets',FilterFeaturedEmojiSetsHook(A))
		except Exception:pass
		B.append('MediaDataController')
KEY_GIFT=3
KEY_VOICE_CHAT=10
KEY_STREAM=11
KEY_STORY=12
class ProfileActionsViewHook(BaseHook):
	def __init__(A,plugin,key_index):super().__init__(plugin);A.key_index=key_index
	def before_hooked_method(A,param):
		C=param;D=bool(A.plugin.get_setting(Keys.hide_profile_actions_gift_button,_B));E=bool(A.plugin.get_setting(Keys.hide_profile_actions_stories_button,_B));F=bool(A.plugin.get_setting(Keys.hide_profile_actions_stream_button,_B))
		if not D and not E and not F:return
		try:B=int(C.args[A.key_index])
		except(IndexError,ValueError,TypeError):return
		G=D and B==KEY_GIFT or E and B==KEY_STORY or F and B in(KEY_VOICE_CHAT,KEY_STREAM)
		if G:C.setResult(_A)
class ProfileActionsApplyHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin)
	def after_hooked_method(B,param):
		try:
			D=bool(B.plugin.get_setting(Keys.hide_profile_actions_gift_button,_B));E=bool(B.plugin.get_setting(Keys.hide_profile_actions_stories_button,_B));F=bool(B.plugin.get_setting(Keys.hide_profile_actions_stream_button,_B))
			if not D and not E and not F:return
			H=param.thisObject;A=[]
			if D:A.append(KEY_GIFT)
			if E:A.append(KEY_STORY)
			if F:A.extend((KEY_VOICE_CHAT,KEY_STREAM))
			for I in('visibleActions','actionsList','allAvailableActions'):
				C=getattr(H,I,_A)
				if C is not _A and hasattr(C,'remove'):
					for G in A:
						for J in(G,jint(G)):
							try:C.remove(J)
							except Exception:pass
		except Exception:pass
class ProfileActivityUpdateBottomButtonYHook(BaseHook):
	def __init__(A,plugin,setting_key):super().__init__(plugin,setting_key);A._last_instance_hash=_A
	def before_hooked_method(A,param):
		B=param
		if A.is_enabled():
			B.setResult(_A)
			try:
				C=B.thisObject;D=C.hashCode()
				if A._last_instance_hash==D:return
				E=get_private_field(C,'bottomButtonsContainer')
				if E:E.setVisibility(8);A._last_instance_hash=D
			except Exception:pass
class ProfileGiftsContainerUpdateButtonHook(BaseHook):
	def __init__(A,plugin,setting_key):super().__init__(plugin,setting_key);A._last_instance_hash=_A
	def before_hooked_method(A,param):
		B=param
		if A.is_enabled():
			B.setResult(_A)
			try:
				C=B.thisObject;D=C.hashCode()
				if A._last_instance_hash==D:return
				E=get_private_field(C,'buttonContainer')
				if E:E.setVisibility(8);A._last_instance_hash=D
			except Exception:pass
class ProfileGiftsContainerGetBottomOffsetHook(BaseHook):
	def before_hooked_method(A,param):
		if A.is_enabled():param.setResult(jint(0))
class ProfileGiftsContainerPageUpdateEmptyViewHook(BaseHook):
	def __init__(A,plugin,setting_key):super().__init__(plugin,setting_key);A._last_instance_hash=_A
	def after_hooked_method(A,param):
		if A.is_enabled():
			try:
				B=param.thisObject;C=B.hashCode()
				if A._last_instance_hash==C:return
				D=get_private_field(B,'emptyView1Button')
				if D:D.setVisibility(8);A._last_instance_hash=C
			except Exception:pass
def register_profile_actions(plugin):
	A=plugin;B=find_class('org.telegram.ui.Components.ProfileActionsView')
	if B:
		try:A.hook_all_methods(B,_E,ProfileActionsViewHook(A,0))
		except Exception:pass
		try:A.hook_all_methods(B,'applyVisibleActions',ProfileActionsApplyHook(A),priority=100)
		except Exception:pass
	D=find_class(_T)
	if D:
		try:A.hook_all_methods(D,'updateBottomButtonY',ProfileActivityUpdateBottomButtonYHook(A,Keys.hide_profile_actions_stories_button))
		except Exception:pass
	C=find_class('org.telegram.ui.Gifts.ProfileGiftsContainer')
	if C:
		try:A.hook_all_methods(C,'updateButton',ProfileGiftsContainerUpdateButtonHook(A,Keys.hide_profile_actions_gift_button))
		except Exception:pass
		try:A.hook_all_methods(C,'getBottomOffset',ProfileGiftsContainerGetBottomOffsetHook(A,Keys.hide_profile_actions_gift_button))
		except Exception:pass
	E=find_class('org.telegram.ui.Gifts.ProfileGiftsContainer$Page')
	if E:
		try:A.hook_all_methods(E,'updateEmptyView',ProfileGiftsContainerPageUpdateEmptyViewHook(A,Keys.hide_profile_actions_gift_button))
		except Exception:pass
class StarGiftPatternsDrawProfileAnimatedPatternHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_A)
class ProfileGiftsViewConstructorHook(BaseHook):
	def after_hooked_method(A,param):
		if A.is_enabled():
			try:param.thisObject.setVisibility(8)
			except Exception:pass
class ProfileGiftsViewUpdateHook(BaseHook):
	def before_hooked_method(B,param):
		A=param
		if B.is_enabled():
			try:A.thisObject.setVisibility(8)
			except Exception:pass
			A.setResult(_A)
class ChatMessageCellSetMessageObjectInternalHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		try:B=param.args[0];B.messageOwner.from_boosts_applied=0
		except(AttributeError,TypeError):pass
class MessagesControllerPeerColorFromCollectibleHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_A)
class UserObjectGetProfileColorIdHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(jint(-1))
class ChatObjectGetProfileColorIdHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(jint(-1))
class DialogObjectGetBotVerificationIconHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(0)
class DialogObjectGetBotVerificationHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_A)
class ProfileActivityGetBotVerificationDrawableHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		param.setResult(_A)
class ChatActivityUpdateTopPanelHook(BaseHook):
	def __init__(A,plugin,setting_key):super().__init__(plugin,setting_key);A._last_instance_hash=_A
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		B=param.thisObject;C=B.hashCode()
		if A._last_instance_hash==C:return
		D=get_private_field(B,_Ar);E=get_private_field(B,'chatInfo')
		if D:D.bot_verification=_A
		if E:E.bot_verification=_A
		A._last_instance_hash=C
class UserFullTLdeserializeHook(BaseHook):
	def after_hooked_method(C,param):
		try:
			B=param.getResult()
			if not B:return
			if C.plugin.get_setting(Keys.hide_profile_music,_B):
				try:B.saved_music=_A
				except Exception as A:XposedBridge.log(f"LiteGram [UserFullHook] error setting saved_music: {A}")
			if C.plugin.get_setting(Keys.hide_profile_business,_B):
				try:B.business_work_hours=_A;B.business_location=_A
				except Exception as A:XposedBridge.log(f"LiteGram [UserFullHook] error setting business fields: {A}")
		except Exception as A:XposedBridge.log(f"LiteGram [UserFullHook] error: {A}")
class NowPlayingControllerShouldShowCardHook(BaseHook):
	def before_hooked_method(A,param):
		if A.is_enabled():param.setResult(_B)
def register_profile_appearance(plugin):
	M='getProfileColorId';A=plugin;D=find_class('org.telegram.ui.Stars.StarGiftPatterns')
	if D:
		try:A.hook_all_methods(D,'drawProfileAnimatedPattern',StarGiftPatternsDrawProfileAnimatedPatternHook(A,Keys.hide_profile_background_emoji))
		except Exception:pass
	B=find_class('org.telegram.ui.Stars.ProfileGiftsView')
	if B:
		try:A.hook_all_constructors(B,ProfileGiftsViewConstructorHook(A,Keys.hide_profile_pinned_gifts))
		except Exception:pass
		try:A.hook_all_methods(B,'update',ProfileGiftsViewUpdateHook(A,Keys.hide_profile_pinned_gifts))
		except Exception:pass
	E=find_class(_BA)
	if E:
		try:A.hook_all_methods(E,'setMessageObjectInternal',ChatMessageCellSetMessageObjectInternalHook(A,Keys.hide_boost_badge))
		except Exception:pass
	F=find_class('org.telegram.messenger.MessagesController$PeerColor');G=find_class(_Q);H=find_class('org.telegram.messenger.ChatObject')
	if F:
		try:A.hook_all_methods(F,'fromCollectible',MessagesControllerPeerColorFromCollectibleHook(A,Keys.hide_profile_colorful_background))
		except Exception:pass
	if G:
		try:A.hook_all_methods(G,M,UserObjectGetProfileColorIdHook(A,Keys.hide_profile_colorful_background))
		except Exception:pass
	if H:
		try:A.hook_all_methods(H,M,ChatObjectGetProfileColorIdHook(A,Keys.hide_profile_colorful_background))
		except Exception:pass
	C=find_class(_R);I=find_class(_T)
	if C:
		try:A.hook_all_methods(C,'getBotVerificationIcon',DialogObjectGetBotVerificationIconHook(A,Keys.hide_bot_verification))
		except Exception:pass
		try:A.hook_all_methods(C,'getBotVerification',DialogObjectGetBotVerificationHook(A,Keys.hide_bot_verification))
		except Exception:pass
	if I:
		try:A.hook_all_methods(I,'getBotVerificationDrawable',ProfileActivityGetBotVerificationDrawableHook(A,Keys.hide_bot_verification))
		except Exception:pass
	J=find_class('org.telegram.tgnet.TLRPC$UserFull')
	if J:
		try:A.hook_all_methods(J,'TLdeserialize',UserFullTLdeserializeHook(A))
		except Exception:pass
	K=find_class('com.exteragram.messenger.nowplaying.NowPlayingController')
	if K:
		try:A.hook_all_methods(K,'shouldShowCard',NowPlayingControllerShouldShowCardHook(A,Keys.hide_profile_music))
		except Exception:pass
	L=find_class(_J)
	if L:
		try:A.hook_all_methods(L,'updateTopPanelView',ChatActivityUpdateTopPanelHook(A,Keys.hide_bot_verification))
		except Exception:pass
PROFILE_SETTINGS_ROW_FIELDS={Keys.hide_premium_row:'premiumRow',Keys.hide_stars_row:'starsRow',Keys.hide_ton_row:'tonRow',Keys.hide_business_row:'businessRow',Keys.hide_send_a_gift_row:'premiumGiftingRow'}
PROFILE_HELP_ROW_FIELDS=['helpHeaderRow','questionRow','policyRow']
PREMIUM_SECTIONS_ROW='premiumSectionsRow'
MODERN_ROW_TO_KEY={11:Keys.hide_premium_row,12:Keys.hide_stars_row,13:Keys.hide_ton_row,15:Keys.hide_business_row,16:Keys.hide_send_a_gift_row,17:Keys.hide_help_section,18:Keys.hide_help_section,19:Keys.hide_help_section,23:Keys.hide_help_section}
MODERN_HELP_ROW_IDS={17,18,19,23}
WALLET_BOT_ID=1985737506
def get_uitem_id(item):
	B=item
	if B is _A:return
	for C in(_N,'f2205id'):
		try:
			A=getattr(B,C,_A)
			if A is not _A:return int(A)
		except Exception:pass
		try:
			A=get_private_field(B,C)
			if A is not _A:return int(A)
		except Exception:pass
def get_uitem_view_type(item):
	B=item
	if B is _A:return
	for C in(_Ap,):
		try:
			A=getattr(B,C,_A)
			if A is not _A:return int(A)
		except Exception:pass
		try:
			A=get_private_field(B,C)
			if A is not _A:return int(A)
		except Exception:pass
def is_wallet_item(item):
	C='object';B=item;D=get_uitem_id(B)
	if D==WALLET_BOT_ID:return _C
	try:A=getattr(B,C,_A)
	except Exception:A=_A
	if A is _A:
		try:A=get_private_field(B,C)
		except Exception:A=_A
	return bool(A and getattr(A,'bot_id',_A)==WALLET_BOT_ID)
_cached_row_fields={}
def _get_valid_row_fields(instance):
	B=instance.getClass()
	if B not in _cached_row_fields:
		D=B.getDeclaredFields();C=[]
		for A in D:
			if A.getType().toString()=='int'and'row'in A.getName().lower()and not A.getModifiers()&Modifier.STATIC:A.setAccessible(_C);C.append(A)
		_cached_row_fields[B]=C
	return _cached_row_fields[B]
class ProfileActivityUpdateRowsIdsHook(BaseHook):
	def __init__(A,plugin):super().__init__(plugin)
	def get_rows_to_remove(B):
		A=[];C=0
		for(D,E)in PROFILE_SETTINGS_ROW_FIELDS.items():
			if B.plugin.get_setting(D,_B):A.append(E);C+=1
		if C==len(PROFILE_SETTINGS_ROW_FIELDS):A.append(PREMIUM_SECTIONS_ROW)
		if B.plugin.get_setting(Keys.hide_help_section,_B):A.extend(PROFILE_HELP_ROW_FIELDS)
		return A
	def before_hooked_method(D,param):
		if not D.plugin.get_setting(Keys.hide_bot_verification,_B):return
		A=param.thisObject;B=get_private_field(A,_Ar);C=get_private_field(A,'chatInfo')
		if B:B.bot_verification=_A
		if C:C.bot_verification=_A
	def after_hooked_method(C,param):
		H=C.get_rows_to_remove()
		if not H:return
		A=param.thisObject;F=A.getClass()
		if not hasattr(C,'_row_count_field_cache'):C._row_count_field_cache={}
		B=C._row_count_field_cache.get(F)
		if not B:
			try:B=F.getDeclaredField(_Ao);B.setAccessible(_C);C._row_count_field_cache[F]=B
			except Exception:pass
		if not B:return
		try:G=B.getInt(A)
		except Exception:return
		I=_get_valid_row_fields(A);J=0
		for M in H:
			E=_A
			for D in I:
				if D.getName()==M:E=D;break
			if E:
				try:
					K=E.getInt(A)
					if K!=-1:
						J+=1;E.setInt(A,jint(-1))
						for D in I:
							L=D.getInt(A)
							if K<L<G:D.setInt(A,jint(L-1))
						G-=1
				except Exception:pass
		if J>0:
			try:B.setInt(A,jint(G))
			except Exception:pass
class SettingsActivityFillItemsHook(BaseHook):
	def after_hooked_method(E,param):
		G=param;B=G.args[0]if G.args else _A
		if B is _A:return
		try:L=B.size()
		except Exception:return
		H=E.plugin.get_setting(Keys.hide_help_section,_B);M=E.plugin.get_setting(Keys.hide_wallet_row,_B);C=set();F=[]
		for A in range(L):
			I=B.get(A);J=get_uitem_id(I);K=MODERN_ROW_TO_KEY.get(J)
			if K and E.plugin.get_setting(K,_B):C.add(A)
			if H and J in MODERN_HELP_ROW_IDS:F.append(A)
			if M and is_wallet_item(I):C.add(A)
		if H and F:
			D=min(F)-1
			while D>=0 and get_uitem_view_type(B.get(D))in(0,7):C.add(D);D-=1
		for A in sorted(C,reverse=_C):
			try:B.remove(A)
			except Exception:pass
class SettingsActivityOnClickHook(BaseHook):
	def before_hooked_method(B,param):
		A=param
		if not A.args:return
		C=A.args[0];E=get_uitem_id(C);D=MODERN_ROW_TO_KEY.get(E)
		if D and B.plugin.get_setting(D,_B):A.setResult(_A);return
		if B.plugin.get_setting(Keys.hide_wallet_row,_B)and is_wallet_item(C):A.setResult(_A)
def register_settings_menu(plugin):
	A=plugin;C=find_class(_T)
	if C:
		try:A.hook_all_methods(C,'updateRowsIds',ProfileActivityUpdateRowsIdsHook(A))
		except Exception:pass
	B=find_class('org.telegram.ui.SettingsActivity')
	if B:
		try:A.hook_all_methods(B,_Aq,SettingsActivityFillItemsHook(A))
		except Exception:pass
		try:A.hook_all_methods(B,'onClick',SettingsActivityOnClickHook(A))
		except Exception:pass
class StarRatingViewSetHook(BaseHook):
	def before_hooked_method(A,param):
		if A.is_enabled():
			try:param.args[0]=_A
			except IndexError:pass
def register_star_rating(plugin):
	A=plugin;B=find_class('org.telegram.ui.Components.StarRatingView')
	if B:
		try:A.hook_all_methods(B,_E,StarRatingViewSetHook(A,Keys.hide_stars_rating))
		except Exception:pass
TL_reactionPaid=find_class('org.telegram.tgnet.TLRPC$TL_reactionPaid')
class ReactionsLayoutInBubbleSetMessageHook(BaseHook):
	def before_hooked_method(D,param):
		if not D.is_enabled():return
		try:E=param.args[0];A=E.messageOwner.reactions.results
		except(AttributeError,TypeError):return
		if A is _A:return
		B=_A
		for F in range(A.size()):
			C=A.get(F);G=C.reaction
			if TL_reactionPaid and isinstance(G,TL_reactionPaid):B=C;break
		if B:A.remove(B)
class ReactionsContainerLayoutSetVisibleReactionsListHook(BaseHook):
	def before_hooked_method(C,param):
		if not C.is_enabled():return
		try:B=param.args[0]
		except IndexError:return
		A=B.size()-1
		while A>=0:
			if B.get(A).isStar:B.remove(A)
			A-=1
class ReactionsContainerLayoutDrawHook(BaseHook):
	def before_hooked_method(A,param):
		if not A.is_enabled():return
		B=param.thisObject;set_private_field(B,'hasStar',_B)
def register_star_reaction(plugin):
	A=plugin;C=find_class('org.telegram.ui.Components.Reactions.ReactionsLayoutInBubble')
	if C:
		try:A.hook_all_methods(C,'setMessage',ReactionsLayoutInBubbleSetMessageHook(A,Keys.hide_star_reaction))
		except Exception:pass
	B=find_class(_BD)
	if B:
		try:A.hook_all_methods(B,'setVisibleReactionsList',ReactionsContainerLayoutSetVisibleReactionsListHook(A,Keys.hide_star_reaction))
		except Exception:pass
		try:A.hook_all_methods(B,'dispatchDraw',ReactionsContainerLayoutDrawHook(A,Keys.hide_star_reaction))
		except Exception:pass
class LiteGramSettingsHeaderHook(BaseHook):
	def __init__(B,plugin):A=plugin;super().__init__(A);B._plugin_ref=weakref.ref(A)
	def after_hooked_method(G,param):
		D=param
		try:
			H=G._plugin_ref()
			if not H:return
			A=D.thisObject;B=D.args[0]
			if not B or B.size()==0:return
			E=get_private_field(A,'plugin')
			if not E or str(E.getId())!='litegram':return
			if get_private_field(A,'createSubFragmentCallback')is not _A:return
			I=get_private_field(A,'searching')
			if I:return
			F=_create_litegram_settings_header(A.getContext())
			if F:
				C=UItem.asCustom(F);C.settingItem=HeaderSetting('litegram_header')
				try:C.setTransparent(_C)
				except Exception:pass
				B.add(0,C);B.add(1,UItem.asShadow())
		except Exception:pass
def _create_litegram_settings_header(context):
	D=context
	try:
		N=find_class('android.widget.FrameLayout');E=N(D);F=find_class('org.telegram.ui.ActionBar.Theme');H=find_class('org.telegram.messenger.AndroidUtilities');I=find_class('org.telegram.ui.Components.LayoutHelper');A=find_class('android.view.Gravity');O=find_class('org.telegram.ui.Components.BackupImageView');G=O(D);G.setRoundRadius(H.dp(16));J=find_class(_An);P=find_class('org.telegram.messenger.ImageLocation')
		def K(img):
			try:
				C,D=__icon__.split('/');B=int(D);A=J.getInstance(0).getStickerSetByName(C)or J.getInstance(0).getStickerSetByEmojiOrName(C)
				if A and A.documents and A.documents.size()>0:B=min(B,A.documents.size()-1);img.setImage(P.getForDocument(A.documents.get(B)),'108_108',_A,_A,0,1);return _C
			except Exception:pass
			return _B
		if not K(G):
			try:Q,R=__icon__.split('/');J.getInstance(0).loadStickersByEmojiOrName(Q,_B,_B);run_on_ui_thread(lambda:K(G),1500)
			except Exception:pass
		E.addView(G,I.createFrame(108,108,A.CENTER|A.TOP,0,20,0,0));L=find_class('android.widget.TextView');M=find_class('android.util.TypedValue');B=L(D);B.setTextColor(F.getColor(F.key_windowBackgroundWhiteBlackText));B.setTypeface(H.getTypeface(H.TYPEFACE_ROBOTO_MEDIUM));B.setTextSize(M.COMPLEX_UNIT_DIP,22);B.setText(__name__);B.setSingleLine(_C);B.setGravity(A.CENTER);E.addView(B,I.createFrame(-2,-2,A.CENTER|A.TOP,50,145,50,0));C=L(D);C.setTextColor(F.getColor(F.key_windowBackgroundWhiteGrayText));C.setTextSize(M.COMPLEX_UNIT_DIP,14);C.setText(t(_o));C.setGravity(A.CENTER);E.addView(C,I.createFrame(-2,-2,A.CENTER|A.TOP,60,180,60,27));return E
	except Exception:return
def register_settings_header(plugin):
	A=plugin;B=find_class('com.exteragram.messenger.plugins.ui.PluginSettingsActivity')
	if B:
		try:C=B.getClass().getDeclaredMethod(_Aq,find_class(_BC),find_class('org.telegram.ui.Components.UniversalAdapter'));C.setAccessible(_C);A.hook_method(C,LiteGramSettingsHeaderHook(A))
		except Exception:pass
SETTINGS_OPTION_ROWS=Keys.SETTINGS_OPTION_ROWS
def _chat_settings():
	A=[Header(text=t(_s)),Text(text=t(_t),link_alias=Keys.hide_stories,on_click=open_extera_tab(Keys.hide_stories),icon=resolve_icon(_D)),Text(text=t(_u),link_alias=Keys.hide_action_bar_status,on_click=open_extera_tab(Keys.hide_action_bar_status),icon=resolve_icon(_D)),Switch(text=t(_v),key=Keys.hide_greeting_button,subtext=t(_w)),Header(text=t(_AH)),Switch(text=t(_AI),key=Keys.hide_action_bar_live_stream),Switch(text=t(_AJ),key=Keys.hide_action_bar_archived_stories),Switch(text=t(_AK),key=Keys.hide_action_bar_send_gift),Switch(text=t(_AL),key=Keys.hide_action_bar_boost_group),Switch(text=t(_AM),key=Keys.hide_action_bar_add_shortcut),Switch(text=t(_AN),key=Keys.hide_action_bar_report),Switch(text=t(_AO),key=Keys.hide_action_bar_clear_history),Header(text=t(_I)),Switch(text=t(_AP),key=Keys.hide_bottom_gift_button),Switch(text=t(_AQ),key=Keys.hide_gift_cards),Switch(text=t(_AR),key=Keys.hide_giveaway_cards),Switch(text=t(_AV),key=Keys.hide_star_reaction),Switch(text=t(_AZ),key=Keys.hide_group_stickers),Switch(text=t(_AW),key=Keys.hide_emoji_search),Header(text=t(_AX)),Text(text=t(_G),link_alias=Keys.switch_all_premium_emoji,on_click=toggle_premium_emoji_options),*[Switch(text=t(B),key=A,default=_B)for(A,B)in Keys.PREMIUM_EMOJI_ROWS],Header(text=t(_AY)),Text(text=t(_G),link_alias=Keys.switch_all_premium_stickers,on_click=toggle_premium_stickers_options),*[Switch(text=t(B),key=A,default=_B)for(A,B)in Keys.PREMIUM_STICKERS_ROWS]];B=_B
	try:
		if parse_version(get_client_version())>=(12,6,0):
			if find_class('com.exteragram.messenger.ai.AiController')is not _A:B=_C
	except Exception:pass
	if B:A.extend([Header(text=t(_Ae)),Switch(text=t(_Af),key=Keys.hide_ai_button),Switch(text=t(_Ag),key=Keys.hide_ai_summarize)])
	return A
def _on_premium_badge_toggle(val):A=val;B=LiteGramPlugin.get_instance();B.set_setting(Keys.hide_premium_badge,A,reload_settings=_B);B.set_setting(Keys.hide_collectible_status,A,reload_settings=_C);show_restart_bulletin(A)
def _profile_settings():
	B=LiteGramPlugin.get_instance();C=bool(B.get_setting(Keys.hide_premium_badge,_B));A=[Header(text=t(_x)),Switch(text=t(_z),key=Keys.hide_profile_actions_stories_button),Switch(text=t(_y),key=Keys.hide_profile_actions_gift_button),Switch(text=t(_A0),key=Keys.hide_profile_actions_stream_button),Header(text=t(_A1)),Switch(text=t(_A2),key=Keys.hide_stories_tab),Switch(text=t(_A3),key=Keys.hide_gifts_tab),Header(text=t(_A4)),Text(text=t(_A5),link_alias=Keys.reply_elements,on_click=open_extera_tab(Keys.reply_elements),icon=resolve_icon(_D)),Switch(text=t(_A6),subtext=t(_A7),key=Keys.hide_profile_background_emoji),Switch(text=t(_A8),key=Keys.hide_profile_pinned_gifts),Switch(text=t(_A9),key=Keys.hide_profile_colorful_background),Switch(text=t(_AA),key=Keys.hide_boost_badge),Switch(text=t(_AB),subtext=t(_AC),on_change=_on_premium_badge_toggle,key=Keys.hide_premium_badge)]
	if not C:A.append(Switch(text=t(_AS),subtext=t(_AT),key=Keys.hide_collectible_status))
	A.extend([Switch(text=t(_AU),key=Keys.hide_stars_rating),Switch(text=t(_AD),key=Keys.hide_bot_verification),Switch(text=t(_AE),key=Keys.hide_profile_music),Switch(text=t(_AF),subtext=t(_AG),key=Keys.hide_profile_business)]);return A
def _interface_settings():
	A=[Header(text=t(_p)),Text(text=t(_G),link_alias=Keys.switch_all,on_click=toggle_settings_options),*[Switch(text=t(B),key=A)for(A,B)in SETTINGS_OPTION_ROWS],Header(text=t(_q)),Text(text=t(_r),link_alias=Keys.drawer_options,on_click=open_extera_tab(Keys.drawer_options),icon=resolve_icon(_D)),Header(text=t(_Aa)),Switch(text=t(_Ab),subtext=t(_Ac),key=Keys.hide_premium_features)]
	try:
		if parse_version(get_client_version())>=(12,9,0):A.append(Switch(text=t(_Ad),key=Keys.hide_articles_editor))
	except Exception:pass
	return A
def _about_settings():return[Header(text=t(_Ah)),Text(text=t(_Ai),icon='msg_link',on_click=open_url_view(GITHUB_URL)),Divider(text=t(_Aj))]
def get_main_settings_list():return[Text(text=t(_k),icon='msg_discussion',create_sub_fragment=_chat_settings,link_alias=Keys.chatSettings),Text(text=t(_l),icon='msg_contacts',create_sub_fragment=_profile_settings,link_alias=Keys.profileSettings),Text(text=t(_m),icon='msg_settings',create_sub_fragment=_interface_settings,link_alias=Keys.interfaceSettings),Text(text=t(_n),icon='msg_help',create_sub_fragment=_about_settings,link_alias=Keys.aboutSettings)]
_PROFILE_GIFT_REQUEST_HOOKS={'TL_users_getFullUser','TL_channels_getFullChannel','TL_messages_getFullChat','TL_contacts_resolveUsername','TL_messages_getDialogs','TL_messages_getPeerDialogs','TL_users_getUsers'}
_GIFT_SUBSTRING_HOOKS={'StarGift','Gifts','StarGiftUnique'}
class LiteGramPlugin(BasePlugin):
	_instance=_A
	def get_setting(A,key,default=_B):
		B=key
		if not hasattr(A,_BK):A._settings_cache={}
		D=time.monotonic();C=A._settings_cache.get(B)
		if C is _A or D-C[1]>1.:E=super().get_setting(B,default);A._settings_cache[B]=E,D;return E
		return C[0]
	def set_setting(D,key,value,reload_settings=_B):
		B=value;A=key
		if hasattr(D,_BK):D._settings_cache[A]=B,time.monotonic()
		super().set_setting(A,B,reload_settings)
		if A==Keys.hide_premium_features and B:
			try:
				E=find_class(_K)
				if E:
					for G in range(4):
						try:
							C=E.getInstance(G)
							if C:F=C.getClass().getDeclaredField(_M);F.setAccessible(_C);F.setBoolean(C,_B)
						except Exception:pass
			except Exception:pass
	def on_plugin_load(A):
		LiteGramPlugin._instance=A;C=['TL_messages_getEmojiStickers','TL_messages_getFeaturedEmojiStickers',_BE,'TL_messages_searchEmojiStickerSets',_BF,'TL_messages_getRecentReactions',_BG,_BH,_BI]
		for B in C:
			try:A.add_hook(B)
			except Exception:pass
		for B in _PROFILE_GIFT_REQUEST_HOOKS:
			try:A.add_hook(B)
			except Exception:pass
		for B in _GIFT_SUBSTRING_HOOKS:
			try:A.add_hook(B,match_substring=_C)
			except Exception:pass
		A.register_hooks()
	def post_request_hook(A,request_name,account,response,error):
		C=request_name;B=response
		if error is not _A or B is _A:return HookResult()
		if A.get_setting(Keys.hide_premium_emoji_keyboard,_B)or A.get_setting(Keys.hide_premium_emoji_reactions,_B)or A.get_setting(Keys.hide_premium_search,_B)or A.get_setting(Keys.hide_premium_suggestions,_B)or A.get_setting(Keys.hide_premium_stickers_recent,_B)or A.get_setting(Keys.hide_premium_stickers_search,_B)or A.get_setting(Keys.hide_premium_stickers_grid,_B)or A.get_setting(Keys.hide_group_stickers,_B):filter_response(C,B)
		if A.get_setting(Keys.hide_gifts_tab,_B):
			if A._is_gift_request(C):A._sanitize_gifts_payload(B)
		return HookResult()
	def on_update_hook(A,update_name,account,update):
		B=update
		if A.get_setting(Keys.hide_gifts_tab,_B)and B is not _A:A._sanitize_gifts_payload(B)
		return HookResult()
	def on_updates_hook(A,container_name,account,updates):
		B=updates
		if A.get_setting(Keys.hide_gifts_tab,_B)and B is not _A:A._sanitize_gifts_payload(B)
		return HookResult()
	def _is_gift_request(B,name):
		if name in _PROFILE_GIFT_REQUEST_HOOKS:return _C
		A=str(name).lower();return'gift'in A or'stargift'in A
	@staticmethod
	def _sanitize_gifts_payload(obj,visited=_A,depth=0):
		K='__iter__';E=depth;C=visited;B=obj
		if B is _A or E>4:return
		if C is _A:C=set()
		try:
			I=id(B)
			if I in C:return
			C.add(I)
		except Exception:pass
		try:
			for L in(_I,'saved_gifts','profileGifts','profile_gifts','profileTabs'):
				F=getattr(B,L,_A)
				if F is not _A and hasattr(F,'clear'):F.clear()
			A=getattr(B,'tabs',_A)
			if A is not _A:
				try:
					if hasattr(A,'size')and hasattr(A,'remove'):
						D=A.size()-1
						while D>=0:
							J=A.get(D)
							if hasattr(J,_N)and J.id==14:A.remove(D)
							D-=1
					elif hasattr(A,K):
						M=[A for A in A if hasattr(A,_N)and A.id==14]
						for N in M:
							try:A.remove(N)
							except Exception:pass
				except Exception:pass
		except Exception:pass
		for G in('users','chats','list',_L):
			try:
				H=getattr(B,G,_A)
				if H is not _A and hasattr(H,K):
					for O in H:LiteGramPlugin._sanitize_gifts_payload(O,C,E+1)
			except Exception:pass
		for G in('user','chat','full_user','full_chat','peer','data','result'):
			try:LiteGramPlugin._sanitize_gifts_payload(getattr(B,G,_A),C,E+1)
			except Exception:pass
	def create_settings(A):return get_main_settings_list()
	def register_hooks(A):register_action_bar(A);register_star_rating(A);register_media_layout(A);register_settings_menu(A);register_gift_button(A);register_gift_cards(A);register_greeting_button(A);register_profile_appearance(A);register_profile_actions(A);register_premium_emoji(A);register_premium_badge(A);register_star_reaction(A);register_hide_emoji_search(A);register_hide_premium_features(A);register_hide_ai_features(A);register_hide_articles(A);register_hide_group_stickers(A);register_settings_header(A)
	@classmethod
	def get_instance(A):
		B='Error while getting LiteGramPlugin Instance!'
		if A._instance is _A:BulletinHelper.show_error(B);raise RuntimeError(B)
		return A._instance


#       @@@@
# @@@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@@
# @@@@@@@@@@@@@@@@
